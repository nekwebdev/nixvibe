---
phase: 21-v0.7-closeout-and-v1.0-pathway
plan: 02
type: execute
wave: 1
depends_on: [21-01]
files_modified:
  - src/nixvibe/orchestrator/v10_pathway_scaffold.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_v10_pathway_scaffold.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Integrate deterministic v1.0 pathway scaffold contract.

## Purpose
Translate v0.7 closeout signals into a machine-readable v1.0 transition scaffold so roadmap progression can move from closeout readiness to v1 planning without ambiguity.

## Output
- `v10_pathway_scaffold` contract (`v10-pathway-scaffold/v1`)
- Pipeline integration at `artifact_summary.v10_pathway_scaffold`
- Deterministic pathway status (`ready`, `hold`, `blocked`) and phase blueprint metadata
- Regression tests for pathway helper and pipeline surface
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/21-v0.7-closeout-and-v1.0-pathway/21-01-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/v07_closeout_evidence.py
@src/nixvibe/orchestrator/governance_hardening_escalation.py
@src/nixvibe/orchestrator/benchmark_release.py
@src/nixvibe/orchestrator/release_policy_execution.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: v1.0 Pathway Scaffold Contract
```gherkin
Given v0.7 closeout and release/governance benchmark signals
When pathway scaffold evaluates
Then deterministic ready/hold/blocked pathway metadata and phase blueprint are emitted
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration artifact assembly
When artifact summary finalizes
Then `artifact_summary.v10_pathway_scaffold` is always present
```

## AC-3: Pathway Status Determinism
```gherkin
Given clear, gated, and blocked transition contexts
When pathway scaffold evaluates
Then pathway status and blockers remain deterministic
```

## AC-4: Regression Stability
```gherkin
Given pathway scaffold integration
When targeted and full suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add v1.0 pathway scaffold helper and exports</name>
  <files>src/nixvibe/orchestrator/v10_pathway_scaffold.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic v1.0 pathway scaffold contract aggregating v0.7 closeout, governance, benchmark release, and release policy signals.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.v10_pathway_scaffold import build_v10_pathway_scaffold"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire v1.0 pathway scaffold in pipeline</name>
  <files>src/nixvibe/orchestrator/pipeline.py, tests/orchestrator/test_acceptance_flows.py</files>
  <action>
    Emit `artifact_summary.v10_pathway_scaffold` from closeout/governance/release readiness signals.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add pathway tests and docs</name>
  <files>tests/orchestrator/test_v10_pathway_scaffold.py, README.md</files>
  <action>
    Add deterministic pathway status tests and document the phase21 v1.0 pathway scaffold slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/21-v0.7-closeout-and-v1.0-pathway/21-02-SUMMARY.md`
</output>
