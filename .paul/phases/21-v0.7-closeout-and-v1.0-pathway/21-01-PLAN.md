---
phase: 21-v0.7-closeout-and-v1.0-pathway
plan: 01
type: execute
wave: 1
depends_on: [20-03]
files_modified:
  - src/nixvibe/orchestrator/v07_closeout_evidence.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_v07_closeout_evidence.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Introduce v0.7 closeout evidence bundle contract.

## Purpose
Package governance and release-policy signals into deterministic v0.7 closeout readiness evidence for milestone completion flow.

## Output
- `v07_closeout_evidence` contract (`v07-closeout-evidence/v1`)
- Pipeline integration at `artifact_summary.v07_closeout_evidence`
- Deterministic closeout readiness categories (`ready`, `hold`, `blocked`)
- Regression tests for closeout evidence paths
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/20-operator-observability-and-governance-hardening/20-03-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/governance_hardening_escalation.py
@src/nixvibe/orchestrator/operator_observability_digest.py
@src/nixvibe/orchestrator/release_policy_execution.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: v0.7 Closeout Evidence Contract
```gherkin
Given governance and release-policy outcomes
When v0.7 closeout evidence evaluates
Then deterministic ready/hold/blocked closeout metadata is emitted
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration artifact assembly
When artifact summary finalizes
Then `artifact_summary.v07_closeout_evidence` is always present
```

## AC-3: Closeout Path Determinism
```gherkin
Given healthy, attention, and blocked governance contexts
When v0.7 closeout evidence evaluates
Then closeout category and blockers remain deterministic
```

## AC-4: Regression Stability
```gherkin
Given v0.7 closeout evidence integration
When targeted and full suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add v0.7 closeout evidence helper and exports</name>
  <files>src/nixvibe/orchestrator/v07_closeout_evidence.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic v0.7 closeout evidence contract aggregating governance hardening escalation and observability/policy execution signals.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.v07_closeout_evidence import build_v07_closeout_evidence"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire v0.7 closeout evidence in pipeline</name>
  <files>src/nixvibe/orchestrator/pipeline.py, tests/orchestrator/test_acceptance_flows.py</files>
  <action>
    Emit `artifact_summary.v07_closeout_evidence` from governance and observability/policy signals.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add closeout evidence tests and docs</name>
  <files>tests/orchestrator/test_v07_closeout_evidence.py, README.md</files>
  <action>
    Add deterministic closeout evidence path tests and document the phase21 closeout evidence slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/21-v0.7-closeout-and-v1.0-pathway/21-01-SUMMARY.md`
</output>
