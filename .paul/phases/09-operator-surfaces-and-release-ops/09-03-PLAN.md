---
phase: 09-operator-surfaces-and-release-ops
plan: 03
type: execute
wave: 1
depends_on: [09-02]
files_modified:
  - tests/orchestrator/test_e2e_operational_acceptance.py
  - README.md
autonomous: true
---

<objective>
## Goal
Harden end-to-end operational acceptance for v0.3 closeout.

## Purpose
Validate full cross-contract behavior (escalation, recovery, run-manifest, release-readiness) for both success and blocked paths.

## Output
- Dedicated end-to-end operational acceptance regression suite
- Cross-contract assertions for release-ready and release-hold journeys
- Documentation update for phase 9 acceptance hardening
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/09-operator-surfaces-and-release-ops/09-02-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/pipeline.py
@src/nixvibe/orchestrator/manifest.py
@src/nixvibe/orchestrator/release.py
</context>

<acceptance_criteria>

## AC-1: Apply Success Journey
```gherkin
Given a successful apply run
When end-to-end acceptance executes
Then run manifest and release readiness both report successful release handoff
```

## AC-2: Blocked Safety Journeys
```gherkin
Given high-risk and critical-conflict apply attempts
When end-to-end acceptance executes
Then mode fallback, safety escalation, and release hold signals are all consistent
```

## AC-3: Validation Failure Journey
```gherkin
Given pre-write validation failure in apply mode
When end-to-end acceptance executes
Then recovery/remediation and release hold outputs are emitted consistently
```

## AC-4: Regression Stability
```gherkin
Given end-to-end acceptance hardening changes
When targeted and full suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add end-to-end acceptance regressions</name>
  <files>tests/orchestrator/test_e2e_operational_acceptance.py</files>
  <action>
    Add complete success and blocked journey scenarios covering run manifest and release readiness outputs.
  </action>
  <verify>python -m unittest tests.orchestrator.test_e2e_operational_acceptance -v</verify>
  <done>AC-1, AC-2, AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Document acceptance hardening</name>
  <files>README.md</files>
  <action>
    Document the end-to-end acceptance coverage and its cross-contract validation scope.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/09-operator-surfaces-and-release-ops/09-03-SUMMARY.md`
</output>
