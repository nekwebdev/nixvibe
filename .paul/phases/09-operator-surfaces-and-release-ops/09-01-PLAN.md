---
phase: 09-operator-surfaces-and-release-ops
plan: 01
type: execute
wave: 1
depends_on: [08-03]
files_modified:
  - src/nixvibe/orchestrator/manifest.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_operator_run_manifest.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Introduce a deterministic operator run-manifest contract for every orchestration run.

## Purpose
Provide operator-facing run metadata in one stable structure to improve daily workflow clarity and prepare release/readiness automation inputs.

## Output
- `run_manifest` contract builder
- Pipeline integration at `artifact_summary.run_manifest`
- Regression tests for mode transitions, specialist outcomes, and safety/validation summaries
- Documentation update for phase 9 manifest surface
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/08-apply-safety-escalation-and-recovery/08-03-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/pipeline.py
@src/nixvibe/orchestrator/guidance.py
@src/nixvibe/orchestrator/recovery.py
@src/nixvibe/orchestrator/guardrails.py
</context>

<acceptance_criteria>

## AC-1: Run Manifest Contract
```gherkin
Given a pipeline execution
When artifact summary is finalized
Then operator run manifest is emitted with stable top-level sections and deterministic fields
```

## AC-2: Mode/Specialist Outcome Coverage
```gherkin
Given apply/propose transitions and mixed specialist outcomes
When run manifest is generated
Then mode transitions and specialist outcome counters are reflected correctly
```

## AC-3: Safety/Validation/Ledger Projection
```gherkin
Given orchestration safety and validation context
When run manifest is emitted
Then manifest includes guardrail/escalation/recovery summaries plus validation/ledger metadata
```

## AC-4: Regression Stability
```gherkin
Given run manifest integration
When targeted and full tests run
Then all tests remain green
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add run manifest builder</name>
  <files>src/nixvibe/orchestrator/manifest.py</files>
  <action>
    Implement deterministic operator run manifest contract helpers.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.manifest import build_operator_run_manifest"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline output</name>
  <files>src/nixvibe/orchestrator/pipeline.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Attach run manifest to artifact summary and export contract helper.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 and AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage and docs</name>
  <files>tests/orchestrator/test_operator_run_manifest.py, tests/orchestrator/test_acceptance_flows.py, README.md</files>
  <action>
    Add dedicated manifest tests and ensure acceptance contracts include run-manifest presence.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/09-operator-surfaces-and-release-ops/09-01-SUMMARY.md`
</output>
