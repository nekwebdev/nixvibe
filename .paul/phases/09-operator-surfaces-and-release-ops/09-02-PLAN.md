---
phase: 09-operator-surfaces-and-release-ops
plan: 02
type: execute
wave: 1
depends_on: [09-01]
files_modified:
  - src/nixvibe/orchestrator/release.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_release_readiness.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Expand release-readiness gates with deterministic pass/fail reporting.

## Purpose
Give operators a stable release-handoff signal with explicit gate failures and remediation direction.

## Output
- `release_readiness` contract builder
- Pipeline emission at `artifact_summary.release_readiness`
- Failure summaries and next gate action field
- Regression tests for release-ready and blocked scenarios
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/09-operator-surfaces-and-release-ops/09-01-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/manifest.py
@src/nixvibe/orchestrator/pipeline.py
@src/nixvibe/orchestrator/escalation.py
@src/nixvibe/orchestrator/recovery.py
@src/nixvibe/orchestrator/guardrails.py
</context>

<acceptance_criteria>

## AC-1: Release Gate Contract
```gherkin
Given a completed run manifest
When release-readiness is evaluated
Then a deterministic contract is emitted with gate-level pass/fail results
```

## AC-2: Required Gate Expansion
```gherkin
Given apply/propose and safety variants
When release-readiness is computed
Then required mode, specialist integrity, safety, validation, and write-materialization gates are evaluated
```

## AC-3: Failure Reporting
```gherkin
Given failed required gates
When release-readiness output is emitted
Then failed gate IDs, failure summaries, and a next gate action are included
```

## AC-4: Regression Stability
```gherkin
Given release-readiness integration
When targeted and full tests execute
Then all tests remain green
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add release-readiness builder</name>
  <files>src/nixvibe/orchestrator/release.py</files>
  <action>
    Implement deterministic release gate evaluation from run-manifest data.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.release import build_release_readiness"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire release-readiness into pipeline</name>
  <files>src/nixvibe/orchestrator/pipeline.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Attach `release_readiness` to artifact summary and export helper.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 and AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage and docs</name>
  <files>tests/orchestrator/test_release_readiness.py, tests/orchestrator/test_acceptance_flows.py, README.md</files>
  <action>
    Add release-ready/blocked scenario tests and document expanded gate contract.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/09-operator-surfaces-and-release-ops/09-02-SUMMARY.md`
</output>
