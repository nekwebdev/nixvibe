---
phase: 08-apply-safety-escalation-and-recovery
plan: 01
type: execute
wave: 1
depends_on: [07-03]
files_modified:
  - src/nixvibe/orchestrator/escalation.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/guidance.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_apply_safety_escalation.py
  - README.md
autonomous: true
---

<objective>
## Goal
Add deterministic apply-safety escalation tiers to orchestration output.

## Purpose
Make apply outcomes explicit and machine-readable so failure/recovery behavior is predictable and auditable.

## Output
- Apply-safety escalation contract builder
- Pipeline integration for escalation summary emission
- Guidance integration for apply-safety tier/reason
- Regression tests for blocked/guarded/advisory escalation cases
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/07-git-ledger-and-change-intelligence/07-03-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/pipeline.py
@src/nixvibe/orchestrator/guidance.py
@src/nixvibe/orchestrator/ledger.py
</context>

<acceptance_criteria>

## AC-1: Escalation Tier Contract
```gherkin
Given apply-mode orchestration state
When escalation rules are evaluated
Then a deterministic tier contract is emitted with stable fields
```

## AC-2: Pipeline Escalation Emission
```gherkin
Given orchestration pipeline completion
When artifact summary is built
Then apply_safety_escalation is included with tier and triggers
```

## AC-3: Guidance Safety Surface
```gherkin
Given guidance summary generation
When apply safety escalation exists
Then guidance includes apply_safety_tier and apply_safety_reason
```

## AC-4: Regression Stability
```gherkin
Given new escalation behavior
When targeted and full unit tests run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Implement escalation tier builder</name>
  <files>src/nixvibe/orchestrator/escalation.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Add deterministic apply-safety escalation contract logic with blocked/guarded/advisory/none tiers.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.escalation import build_apply_safety_escalation"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline and guidance</name>
  <files>src/nixvibe/orchestrator/pipeline.py, src/nixvibe/orchestrator/guidance.py</files>
  <action>
    Emit escalation summary in artifact contract and project tier/reason into guidance output.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 and AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests and docs</name>
  <files>tests/orchestrator/test_apply_safety_escalation.py, README.md</files>
  <action>
    Add escalation regression tests and document phase-8 escalation contract behavior.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/08-apply-safety-escalation-and-recovery/08-01-SUMMARY.md`
</output>
