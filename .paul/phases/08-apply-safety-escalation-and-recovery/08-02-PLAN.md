---
phase: 08-apply-safety-escalation-and-recovery
plan: 02
type: execute
wave: 1
depends_on: [08-01]
files_modified:
  - src/nixvibe/orchestrator/recovery.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/guidance.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_guidance_remediation.py
  - tests/orchestrator/test_recovery_playbook.py
  - README.md
autonomous: true
---

<objective>
## Goal
Integrate deterministic recovery playbooks for apply-time safety escalation outcomes.

## Purpose
Ensure blocked/guarded/advisory apply paths always emit structured, reversible remediation guidance beyond single next-action text.

## Output
- Recovery playbook contract builder
- Pipeline integration for `artifact_summary.recovery_playbook`
- Guidance projection for recovery stage/strategy metadata
- Regression tests for recovery playbook behavior and guidance wiring
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/08-apply-safety-escalation-and-recovery/08-01-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/escalation.py
@src/nixvibe/orchestrator/pipeline.py
@src/nixvibe/orchestrator/guidance.py
</context>

<acceptance_criteria>

## AC-1: Recovery Playbook Contract
```gherkin
Given apply-safety escalation outcomes
When recovery policy is evaluated
Then a deterministic playbook is emitted with stage, strategy, and reversible actions
```

## AC-2: Pipeline Recovery Emission
```gherkin
Given orchestration pipeline completion
When artifact summary is produced
Then recovery_playbook is included with stable fields
```

## AC-3: Guidance Recovery Surface
```gherkin
Given guidance summary generation
When recovery playbook exists
Then guidance includes recovery requirement/stage/strategy fields
```

## AC-4: Regression Stability
```gherkin
Given recovery integration
When targeted and full tests are executed
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add recovery playbook builder</name>
  <files>src/nixvibe/orchestrator/recovery.py</files>
  <action>
    Implement deterministic recovery plans for pre-write, post-write, conflict, advisory, and no-escalation outcomes.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.recovery import build_recovery_playbook"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire recovery into pipeline and guidance</name>
  <files>src/nixvibe/orchestrator/pipeline.py, src/nixvibe/orchestrator/guidance.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Attach recovery playbook to artifact summary and expose key recovery fields in guidance output.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 and AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests and docs</name>
  <files>tests/orchestrator/test_recovery_playbook.py, tests/orchestrator/test_guidance_remediation.py, README.md</files>
  <action>
    Add focused tests for recovery contract behavior and document the recovery playbook integration.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/08-apply-safety-escalation-and-recovery/08-02-SUMMARY.md`
</output>
