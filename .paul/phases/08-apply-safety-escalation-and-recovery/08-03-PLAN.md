---
phase: 08-apply-safety-escalation-and-recovery
plan: 03
type: execute
wave: 1
depends_on: [08-02]
files_modified:
  - src/nixvibe/orchestrator/guardrails.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/escalation.py
  - src/nixvibe/orchestrator/recovery.py
  - src/nixvibe/orchestrator/guidance.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_high_risk_guardrails.py
  - README.md
autonomous: true
---

<objective>
## Goal
Add deterministic high-risk mutation guardrails to block unsafe apply mutations.

## Purpose
Ensure apply mode cannot proceed when specialist outputs indicate irreversible actions or critical risk posture.

## Output
- High-risk mutation guardrail contract
- Pipeline enforcement of guardrail-driven apply-to-propose fallback
- Escalation/recovery/guidance integration for guardrail outcomes
- Regression tests for guarded and non-guarded apply paths
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/08-apply-safety-escalation-and-recovery/08-02-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/escalation.py
@src/nixvibe/orchestrator/recovery.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Guardrail Contract
```gherkin
Given specialist payload recommendations and risks
When high-risk mutation guardrails are evaluated
Then deterministic guardrail metadata is emitted with triggers and apply-block signal
```

## AC-2: Apply Safety Enforcement
```gherkin
Given apply mode with high-risk guardrail triggers
When pipeline resolves final mode
Then apply is forced to propose before write path execution
```

## AC-3: Integrated Recovery/Guidance Signal
```gherkin
Given guardrail-forced propose outcome
When escalation and guidance summaries are emitted
Then high-risk remediation and recovery stage are explicit
```

## AC-4: Regression Stability
```gherkin
Given high-risk guardrail integration
When targeted and full tests execute
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Implement guardrail evaluator</name>
  <files>src/nixvibe/orchestrator/guardrails.py</files>
  <action>
    Add high-risk trigger evaluation for irreversible recommendations and critical risks.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.guardrails import evaluate_high_risk_mutation_guardrails"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate guardrails into pipeline/escalation/recovery/guidance</name>
  <files>src/nixvibe/orchestrator/pipeline.py, src/nixvibe/orchestrator/escalation.py, src/nixvibe/orchestrator/recovery.py, src/nixvibe/orchestrator/guidance.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Enforce propose fallback for high-risk apply attempts and surface structured guardrail remediation metadata.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 and AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add regressions and docs</name>
  <files>tests/orchestrator/test_high_risk_guardrails.py, README.md</files>
  <action>
    Add regression coverage for high-risk blocked and safe apply paths and document the guardrail contract.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/08-apply-safety-escalation-and-recovery/08-03-SUMMARY.md`
</output>
