---
phase: 11-operator-policy-and-controls
plan: 02
type: execute
wave: 1
depends_on: [11-01]
files_modified:
  - src/nixvibe/orchestrator/override.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_controlled_override_workflow.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Add controlled override workflow contract.

## Purpose
Expose explicit allow/deny override decisions with guardrails so policy exceptions stay bounded and auditable.

## Output
- `controlled_override_workflow` contract builder
- Pipeline emission at `artifact_summary.controlled_override_workflow`
- Deterministic override request parsing + policy decisions
- Regression tests for allowed and denied override paths
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/11-operator-policy-and-controls/11-01-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/explainability.py
@src/nixvibe/orchestrator/retry.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Controlled Override Contract
```gherkin
Given user override language and runtime safety state
When override workflow is evaluated
Then deterministic override decision and blockers are emitted
```

## AC-2: Guardrail Enforcement
```gherkin
Given unsafe override requests (for example validation bypass)
When override workflow is evaluated
Then requests are denied with explicit blocker reasons
```

## AC-3: Pipeline Integration
```gherkin
Given orchestration output
When artifact summary is finalized
Then `controlled_override_workflow` is included in output contract
```

## AC-4: Regression Stability
```gherkin
Given override workflow integration
When targeted and full tests run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add controlled override helper</name>
  <files>src/nixvibe/orchestrator/override.py</files>
  <action>
    Implement deterministic override request parsing and allow/deny workflow with explicit blockers and required checks.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.override import build_controlled_override_workflow"</verify>
  <done>AC-1 and AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline output</name>
  <files>src/nixvibe/orchestrator/pipeline.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Emit `artifact_summary.controlled_override_workflow` and export helper.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests and docs</name>
  <files>tests/orchestrator/test_controlled_override_workflow.py, tests/orchestrator/test_acceptance_flows.py, README.md</files>
  <action>
    Add regression coverage and document controlled override workflow contract.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/11-operator-policy-and-controls/11-02-SUMMARY.md`
</output>
