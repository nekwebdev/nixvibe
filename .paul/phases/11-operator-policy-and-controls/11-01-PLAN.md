---
phase: 11-operator-policy-and-controls
plan: 01
type: execute
wave: 1
depends_on: [10-03]
files_modified:
  - src/nixvibe/orchestrator/explainability.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_policy_decision_explainability.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Add policy decision explainability contract.

## Purpose
Make route/mode/safety/release decisions explicit and deterministic for operator diagnostics and downstream policy consumers.

## Output
- `policy_decision_explainability` contract builder
- Pipeline emission at `artifact_summary.policy_decision_explainability`
- Stage-by-stage decision trace with reasons/signals
- Regression tests for success, critical-conflict, and high-risk guardrail paths
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/10-runtime-reliability-and-resume/10-03-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/router.py
@src/nixvibe/orchestrator/modes.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Explainability Contract
```gherkin
Given pipeline policy decisions
When explainability output is generated
Then deterministic stage decisions and reasons are emitted
```

## AC-2: Conflict Priority Transparency
```gherkin
Given configured conflict priority ordering
When explainability output is generated
Then conflict priority order is explicitly surfaced
```

## AC-3: Pipeline Integration
```gherkin
Given orchestration output
When artifact summary is finalized
Then `policy_decision_explainability` is included in output contract
```

## AC-4: Regression Stability
```gherkin
Given explainability integration
When targeted and full tests run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add explainability contract helper</name>
  <files>src/nixvibe/orchestrator/explainability.py</files>
  <action>
    Implement deterministic decision trace contract with stage decisions, reasons, and policy priority ordering.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.explainability import build_policy_decision_explainability"</verify>
  <done>AC-1 and AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline output</name>
  <files>src/nixvibe/orchestrator/pipeline.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Emit `artifact_summary.policy_decision_explainability` and export helper.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests and docs</name>
  <files>tests/orchestrator/test_policy_decision_explainability.py, tests/orchestrator/test_acceptance_flows.py, README.md</files>
  <action>
    Add regression coverage and document explainability contract.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/11-operator-policy-and-controls/11-01-SUMMARY.md`
</output>
