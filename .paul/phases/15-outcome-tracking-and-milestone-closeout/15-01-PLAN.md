---
phase: 15-outcome-tracking-and-milestone-closeout
plan: 01
type: execute
wave: 1
depends_on: [14-03]
files_modified:
  - src/nixvibe/orchestrator/outcome_scorecard.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_outcome_scorecard.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Add deterministic outcome scorecard contract for phase 15 kickoff.

## Purpose
Translate benchmark/baseline/regression signals into milestone success-metric scorecards used for closeout confidence.

## Output
- `outcome_scorecard` contract helper (`outcome-scorecard/v1`)
- Metric scoring tied to v0.5 success targets (scaffold time, modularization safety, service-add confidence)
- Pipeline integration at `artifact_summary.outcome_scorecard`
- Regression tests for on-track, watch, and at-risk scorecard paths
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/14-benchmark-scenario-harness-and-reports/14-03-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/benchmark.py
@src/nixvibe/orchestrator/benchmark_runner.py
@src/nixvibe/orchestrator/benchmark_snapshot.py
@src/nixvibe/orchestrator/release.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Outcome Scorecard Contract
```gherkin
Given benchmark and release context contracts
When outcome scorecard is built
Then deterministic metric scores and overall status are emitted
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration output assembly
When artifact summary is finalized
Then `artifact_summary.outcome_scorecard` is always present
```

## AC-3: Deterministic Score Paths
```gherkin
Given on-track, watch, and at-risk benchmark contexts
When scorecard contract is evaluated
Then metric pass/fail and overall status are deterministic
```

## AC-4: Regression Stability
```gherkin
Given outcome scorecard integration
When targeted and full test suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add outcome scorecard helper and exports</name>
  <files>src/nixvibe/orchestrator/outcome_scorecard.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic metric scoring + overall status computation with dependency contract checks.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.outcome_scorecard import build_outcome_scorecard"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline outcome scorecard output</name>
  <files>src/nixvibe/orchestrator/pipeline.py</files>
  <action>
    Emit `artifact_summary.outcome_scorecard` after baseline snapshot and release/telemetry contracts are available.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests/docs and verify full suite</name>
  <files>tests/orchestrator/test_outcome_scorecard.py, tests/orchestrator/test_acceptance_flows.py, README.md</files>
  <action>
    Add deterministic score-path tests and document phase 15 outcome scorecard slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/15-outcome-tracking-and-milestone-closeout/15-01-SUMMARY.md`
</output>
