---
phase: 15-outcome-tracking-and-milestone-closeout
plan: 02
type: execute
wave: 1
depends_on: [15-01]
files_modified:
  - src/nixvibe/orchestrator/benchmark_release.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_benchmark_release_readiness.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Integrate benchmark-aware release readiness contract for phase 15 plan 15-02.

## Purpose
Combine base release gates with outcome scorecard and benchmark snapshot signals so closeout readiness reflects benchmark health.

## Output
- `benchmark_release_readiness` contract helper (`benchmark-release-readiness/v1`)
- Deterministic gates over release readiness, outcome score, regression state, baseline candidate, and runner readiness
- Pipeline integration at `artifact_summary.benchmark_release_readiness`
- Regression tests for benchmark-ready and blocked paths
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/15-outcome-tracking-and-milestone-closeout/15-01-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/release.py
@src/nixvibe/orchestrator/outcome_scorecard.py
@src/nixvibe/orchestrator/benchmark_snapshot.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Benchmark-Aware Release Readiness Contract
```gherkin
Given release readiness and benchmark outcome context
When benchmark release readiness is built
Then deterministic gate results and benchmark-aware readiness status are emitted
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration output assembly
When artifact summary is finalized
Then `artifact_summary.benchmark_release_readiness` is always present
```

## AC-3: Deterministic Ready/Blocked Paths
```gherkin
Given healthy, propose-mode, and regression contexts
When benchmark release readiness is evaluated
Then readiness and failed gate IDs are deterministic
```

## AC-4: Regression Stability
```gherkin
Given benchmark-aware release integration
When targeted and full test suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add benchmark release readiness helper and exports</name>
  <files>src/nixvibe/orchestrator/benchmark_release.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic benchmark-aware gate composition on top of release readiness + outcome scorecard + snapshot signals.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.benchmark_release import build_benchmark_release_readiness"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline benchmark release readiness output</name>
  <files>src/nixvibe/orchestrator/pipeline.py</files>
  <action>
    Emit `artifact_summary.benchmark_release_readiness` after release/outcome/snapshot contracts are available.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests/docs and verify full suite</name>
  <files>tests/orchestrator/test_benchmark_release_readiness.py, tests/orchestrator/test_acceptance_flows.py, README.md</files>
  <action>
    Add deterministic ready/blocked tests and document phase 15 benchmark-aware release slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/15-outcome-tracking-and-milestone-closeout/15-02-SUMMARY.md`
</output>
