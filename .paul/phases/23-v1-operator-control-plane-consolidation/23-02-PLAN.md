---
phase: 23-v1-operator-control-plane-consolidation
plan: 02
type: execute
wave: 1
depends_on: [23-01]
files_modified:
  - src/nixvibe/orchestrator/governance_workflow_consolidation.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_governance_workflow_consolidation.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Introduce governance workflow consolidation integration contract for v1 phase23.

## Purpose
Consolidate control-plane, governance escalation, override workflow, and release policy execution signals into deterministic governance workflow consolidation metadata.

## Output
- `governance_workflow_consolidation` contract (`governance-workflow-consolidation/v1`)
- Pipeline integration at `artifact_summary.governance_workflow_consolidation`
- Deterministic consolidation status (`consolidated`, `review`, `blocked`)
- Regression tests for consolidation helper and pipeline surface
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/23-v1-operator-control-plane-consolidation/23-01-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/operator_control_plane_summary.py
@src/nixvibe/orchestrator/governance_hardening_escalation.py
@src/nixvibe/orchestrator/override.py
@src/nixvibe/orchestrator/release_policy_execution.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Governance Workflow Consolidation Contract
```gherkin
Given operator control-plane and governance workflow signals
When consolidation evaluates
Then deterministic consolidated/review/blocked governance workflow metadata is emitted
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration artifact assembly
When artifact summary finalizes
Then `artifact_summary.governance_workflow_consolidation` is always present
```

## AC-3: Status Determinism
```gherkin
Given stable, review, and blocked contexts
When governance workflow consolidation evaluates
Then status and blockers remain deterministic
```

## AC-4: Regression Stability
```gherkin
Given governance workflow consolidation integration
When targeted and full suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add governance workflow consolidation helper and exports</name>
  <files>src/nixvibe/orchestrator/governance_workflow_consolidation.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic governance workflow consolidation contract from control-plane, governance escalation, override, and release policy execution surfaces.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.governance_workflow_consolidation import build_governance_workflow_consolidation"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire governance workflow consolidation in pipeline</name>
  <files>src/nixvibe/orchestrator/pipeline.py, tests/orchestrator/test_acceptance_flows.py</files>
  <action>
    Emit `artifact_summary.governance_workflow_consolidation` from control-plane and governance workflow outputs.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add governance workflow consolidation tests and docs</name>
  <files>tests/orchestrator/test_governance_workflow_consolidation.py, README.md</files>
  <action>
    Add deterministic consolidation status tests and document phase23-02 consolidation slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/23-v1-operator-control-plane-consolidation/23-02-SUMMARY.md`
</output>
