---
phase: 23-v1-operator-control-plane-consolidation
plan: 01
type: execute
wave: 1
depends_on: [22-03]
files_modified:
  - src/nixvibe/orchestrator/operator_control_plane_summary.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_operator_control_plane_summary.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Introduce operator control-plane summary contract for v1 phase23.

## Purpose
Consolidate migration policy, governance posture, audit trail, and explainability signals into deterministic operator-facing control-plane summary metadata.

## Output
- `operator_control_plane_summary` contract (`operator-control-plane-summary/v1`)
- Pipeline integration at `artifact_summary.operator_control_plane_summary`
- Deterministic control-plane status (`aligned`, `attention`, `blocked`)
- Regression tests for summary helper and pipeline surface
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/22-v1-foundation-hardening-and-compatibility/22-03-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/migration_safety_policy.py
@src/nixvibe/orchestrator/governance_hardening_escalation.py
@src/nixvibe/orchestrator/audittrail.py
@src/nixvibe/orchestrator/explainability.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Operator Control-Plane Summary Contract
```gherkin
Given migration policy, governance posture, and operator diagnostics
When control-plane summary evaluates
Then deterministic aligned/attention/blocked control-plane metadata is emitted
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration artifact assembly
When artifact summary finalizes
Then `artifact_summary.operator_control_plane_summary` is always present
```

## AC-3: Status Determinism
```gherkin
Given stable, attention, and blocked contexts
When control-plane summary evaluates
Then status and blockers remain deterministic
```

## AC-4: Regression Stability
```gherkin
Given control-plane summary integration
When targeted and full suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add operator control-plane summary helper and exports</name>
  <files>src/nixvibe/orchestrator/operator_control_plane_summary.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic operator control-plane summary contract from migration safety policy, governance, audit trail, and explainability surfaces.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.operator_control_plane_summary import build_operator_control_plane_summary"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire control-plane summary in pipeline</name>
  <files>src/nixvibe/orchestrator/pipeline.py, tests/orchestrator/test_acceptance_flows.py</files>
  <action>
    Emit `artifact_summary.operator_control_plane_summary` from migration/governance/operator diagnostics outputs.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add control-plane tests and docs</name>
  <files>tests/orchestrator/test_operator_control_plane_summary.py, README.md</files>
  <action>
    Add deterministic control-plane status tests and document phase23 summary slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/23-v1-operator-control-plane-consolidation/23-01-SUMMARY.md`
</output>
