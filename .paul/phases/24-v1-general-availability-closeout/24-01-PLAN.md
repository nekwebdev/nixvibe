---
phase: 24-v1-general-availability-closeout
plan: 01
type: execute
wave: 1
depends_on: [23-03]
files_modified:
  - src/nixvibe/orchestrator/v10_launch_evidence_bundle.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_v10_launch_evidence_bundle.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Introduce v1 launch evidence bundle contract for phase24.

## Purpose
Package operator consolidation, governance posture, and benchmark release readiness into deterministic launch evidence metadata for GA closeout.

## Output
- `v10_launch_evidence_bundle` contract (`v10-launch-evidence-bundle/v1`)
- Pipeline integration at `artifact_summary.v10_launch_evidence_bundle`
- Deterministic evidence status (`ready`, `hold`, `blocked`)
- Regression tests for launch evidence helper and pipeline surface
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/23-v1-operator-control-plane-consolidation/23-03-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/governance_workflow_consolidation.py
@src/nixvibe/orchestrator/operator_control_plane_summary.py
@src/nixvibe/orchestrator/benchmark_release.py
@src/nixvibe/orchestrator/release_policy_execution.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: v1 Launch Evidence Bundle Contract
```gherkin
Given operator consolidation and benchmark release context
When launch evidence evaluates
Then deterministic ready/hold/blocked launch evidence metadata is emitted
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration artifact assembly
When artifact summary finalizes
Then `artifact_summary.v10_launch_evidence_bundle` is always present
```

## AC-3: Evidence Determinism
```gherkin
Given stable, hold, and blocked launch contexts
When launch evidence evaluates
Then status and blockers remain deterministic
```

## AC-4: Regression Stability
```gherkin
Given launch evidence integration
When targeted and full suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add launch evidence helper and exports</name>
  <files>src/nixvibe/orchestrator/v10_launch_evidence_bundle.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic launch evidence bundle contract from governance consolidation, control-plane, benchmark release readiness, and release policy execution.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.v10_launch_evidence_bundle import build_v10_launch_evidence_bundle"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire launch evidence in pipeline</name>
  <files>src/nixvibe/orchestrator/pipeline.py, tests/orchestrator/test_acceptance_flows.py</files>
  <action>
    Emit `artifact_summary.v10_launch_evidence_bundle` from phase23 and benchmark release outputs.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add launch evidence tests and docs</name>
  <files>tests/orchestrator/test_v10_launch_evidence_bundle.py, README.md</files>
  <action>
    Add deterministic launch evidence tests and document phase24 launch evidence slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/24-v1-general-availability-closeout/24-01-SUMMARY.md`
</output>
