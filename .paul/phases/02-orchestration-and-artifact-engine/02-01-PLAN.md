---
phase: 02-orchestration-and-artifact-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/nixvibe/orchestrator/types.py
  - src/nixvibe/orchestrator/policy_loader.py
  - src/nixvibe/orchestrator/router.py
  - src/nixvibe/orchestrator/modes.py
  - src/nixvibe/orchestrator/conflicts.py
  - tests/orchestrator/test_route_and_modes.py
  - tests/orchestrator/test_conflict_priority.py
  - README.md
autonomous: false
---

<objective>
## Goal
Implement the first runtime orchestration slice: deterministic route selection and write-mode gating, driven by locked Phase 1 contracts.

## Purpose
Translate contract docs into executable behavior so nixvibe can choose `init` vs `audit` safely and enforce mode rules before broader specialist orchestration is added.

## Output
Working orchestration core modules plus tests for route/mode/conflict-policy behavior, and documentation updates describing runtime behavior.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@.agents/carl/nixvibe-domain.md
@docs/contracts/local-mcp-contract.md
@docs/contracts/specialist-output-schema.md

## Prior Work
@.paul/phases/01-core-policy-and-contracts/01-01-SUMMARY.md

## Source Files
@README.md
</context>

<acceptance_criteria>

## AC-1: Deterministic Route Selection
```gherkin
Given a user request and repository context
When orchestration route selection runs
Then it deterministically returns `init` or `audit` according to CARL policy
```

## AC-2: Write-Mode Gating Enforced
```gherkin
Given orchestration is preparing a response
When mode resolution runs
Then `audit` defaults to `propose`, `apply` requires explicit opt-in, and no surprise writes are allowed
```

## AC-3: Conflict Priority Resolver Works
```gherkin
Given specialist recommendations with conflicting priorities
When merge policy applies
Then conflict resolution uses fixed order `safety > correctness > reversibility > simplicity > user preference > style`
```

## AC-4: Contract-Driven Runtime Coverage
```gherkin
Given the Phase 1 contract artifacts
When route/mode/conflict tests execute
Then tests verify behavior against contract-defined rules and fail on policy drift
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Implement policy-backed orchestration primitives</name>
  <files>src/nixvibe/orchestrator/types.py, src/nixvibe/orchestrator/policy_loader.py, src/nixvibe/orchestrator/router.py, src/nixvibe/orchestrator/modes.py, src/nixvibe/orchestrator/conflicts.py</files>
  <action>
    Create orchestration module structure and implement:
    - typed request/context models
    - CARL policy loader (from `.agents/carl/nixvibe-domain.md`) with explicit parse/validation error surfaces
    - route selector (`init` / `audit`) based on CARL route policy
    - mode resolver (`advice` / `propose` / `apply`) with confirm-first guardrails
    - conflict resolver with fixed priority ordering and deterministic tie behavior
    Keep implementation minimal and contract-driven. Avoid adding specialist execution logic in this plan.
  </action>
  <verify>test -f src/nixvibe/orchestrator/router.py && test -f src/nixvibe/orchestrator/modes.py && test -f src/nixvibe/orchestrator/conflicts.py</verify>
  <done>AC-1, AC-2, AC-3 satisfied at implementation level and ready for behavioral tests.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Phase 2 runtime work requires local CARL installation for both Codex and Claude before execution proceeds.</what-built>
  <how-to-verify>
    1. Install your local CARL setup into this repo for Codex and Claude.
    2. Verify CARL-related local files/services are available for this project context.
    3. Confirm installation is complete.
  </how-to-verify>
  <resume-signal>Type "done" after CARL is installed locally for both tools.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Add contract-linked tests and runtime docs update</name>
  <files>tests/orchestrator/test_route_and_modes.py, tests/orchestrator/test_conflict_priority.py, README.md</files>
  <action>
    Add tests that assert:
    - route decisions follow contract rules
    - audit default mode is `propose` and apply requires explicit opt-in
    - conflict merge order is fixed and deterministic
    Include fixtures/examples derived from Phase 1 contract documents.
    Update README orchestration section to reflect implemented runtime behavior and CARL dependency timing.
  </action>
  <verify>test -f tests/orchestrator/test_route_and_modes.py && test -f tests/orchestrator/test_conflict_priority.py</verify>
  <done>AC-4 satisfied with passing contract-linked tests and updated documentation.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Phase 1 contract artifacts semantics (`.agents/carl/nixvibe-domain.md`, `docs/contracts/*`) unless drift bug is found and explicitly documented
- Phase 3 validation/acceptance implementation scope
- Git history/commit structure from prior phase

## SCOPE LIMITS
- No specialist parallel execution engine yet (planned in 02-02)
- No scaffold/refactor artifact emission pipeline yet (planned in 02-03)
- No UI or external integration beyond local CARL/MCP expectations

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Route selector returns deterministic `init`/`audit` for defined scenarios
- [ ] Mode resolver enforces confirm-first constraints and no-surprise-write policy
- [ ] Conflict resolver honors fixed priority order
- [ ] Tests exist for route/mode/conflict behavior and are runnable
- [ ] README reflects runtime behavior and CARL dependency timing
</verification>

<success_criteria>
- Runtime primitives implemented for route/mode/conflict policy
- CARL local installation checkpoint completed
- Contract-linked tests and docs updated
- Plan output ready for Phase 2 continuation (`02-02`)
</success_criteria>

<output>
After completion, create `.paul/phases/02-orchestration-and-artifact-engine/02-01-SUMMARY.md`
</output>

