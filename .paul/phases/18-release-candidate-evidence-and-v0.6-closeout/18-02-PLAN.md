---
phase: 18-release-candidate-evidence-and-v0.6-closeout
plan: 02
type: execute
wave: 1
depends_on: [18-01]
files_modified:
  - src/nixvibe/orchestrator/v06_readiness_summary.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_v06_readiness_summary.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Integrate operator-facing v0.6 readiness summary contract.

## Purpose
Advance phase 18 by exposing a single deterministic readiness summary derived from release candidate evidence and key upstream contracts.

## Output
- `v06_readiness_summary` contract (`v06-readiness-summary/v1`)
- Pipeline integration at `artifact_summary.v06_readiness_summary`
- Deterministic readiness bands (`ready`, `hold`, `blocked`) for operator handoff
- Regression tests for readiness summary scenarios
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/18-release-candidate-evidence-and-v0.6-closeout/18-01-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/release_candidate_evidence.py
@src/nixvibe/orchestrator/release.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: v0.6 Readiness Summary Contract
```gherkin
Given release candidate evidence and key release contracts
When readiness summary is built
Then deterministic readiness band, blockers, and next-action metadata are emitted
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration artifact assembly
When artifact summary is finalized
Then `artifact_summary.v06_readiness_summary` is always present
```

## AC-3: Summary Readiness Paths
```gherkin
Given ready, hold, and blocked evidence contexts
When readiness summary evaluates
Then readiness band and blocker summaries are deterministic
```

## AC-4: Regression Stability
```gherkin
Given readiness summary integration
When targeted and full suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add v0.6 readiness summary helper and exports</name>
  <files>src/nixvibe/orchestrator/v06_readiness_summary.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic readiness summary contract from release candidate evidence and release-check surfaces.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.v06_readiness_summary import build_v06_readiness_summary"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire v0.6 readiness summary in pipeline</name>
  <files>src/nixvibe/orchestrator/pipeline.py, tests/orchestrator/test_acceptance_flows.py</files>
  <action>
    Emit `artifact_summary.v06_readiness_summary` from release-candidate evidence and downstream release-check context.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add readiness summary tests and docs</name>
  <files>tests/orchestrator/test_v06_readiness_summary.py, README.md</files>
  <action>
    Add deterministic readiness-summary path tests and document phase18 readiness summary slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/18-release-candidate-evidence-and-v0.6-closeout/18-02-SUMMARY.md`
</output>
