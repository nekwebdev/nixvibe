---
phase: 18-release-candidate-evidence-and-v0.6-closeout
plan: 01
type: execute
wave: 1
depends_on: [17-03]
files_modified:
  - src/nixvibe/orchestrator/release_candidate_evidence.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_release_candidate_evidence.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Introduce deterministic release-candidate evidence bundle contract.

## Purpose
Start phase 18 by packaging milestone-ready evidence from release, alert, and policy contracts into one operator-facing bundle.

## Output
- `release_candidate_evidence` contract (`release-candidate-evidence/v1`)
- Pipeline integration at `artifact_summary.release_candidate_evidence`
- Deterministic readiness categories and evidence checklist semantics
- Regression tests for ready/hold/blocked evidence bundle paths
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/17-outcome-policy-gates-and-alert-escalation/17-03-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/release.py
@src/nixvibe/orchestrator/outcome_alert.py
@src/nixvibe/orchestrator/alert_policy_gate.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Release Candidate Evidence Contract
```gherkin
Given release, alert, and policy contracts
When release candidate evidence is built
Then deterministic readiness category, checklist, and next-action metadata are emitted
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration artifact assembly
When artifact summary is finalized
Then `artifact_summary.release_candidate_evidence` is always present
```

## AC-3: Evidence Readiness Paths
```gherkin
Given ready, hold, and blocked contexts
When evidence bundle evaluates contracts
Then readiness category and evidence checklist outcomes are deterministic
```

## AC-4: Regression Stability
```gherkin
Given release-candidate evidence integration
When targeted and full suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add release candidate evidence helper and exports</name>
  <files>src/nixvibe/orchestrator/release_candidate_evidence.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic release-candidate evidence contract from release readiness, outcome alert, and alert policy gate contracts.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.release_candidate_evidence import build_release_candidate_evidence"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire release candidate evidence in pipeline</name>
  <files>src/nixvibe/orchestrator/pipeline.py, tests/orchestrator/test_acceptance_flows.py</files>
  <action>
    Emit `artifact_summary.release_candidate_evidence` for downstream closeout/readiness summary plans.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add evidence-path tests and docs</name>
  <files>tests/orchestrator/test_release_candidate_evidence.py, README.md</files>
  <action>
    Add deterministic release-candidate evidence path tests and document phase18 evidence slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/18-release-candidate-evidence-and-v0.6-closeout/18-01-SUMMARY.md`
</output>
