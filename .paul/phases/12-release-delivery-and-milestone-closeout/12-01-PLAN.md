---
phase: 12-release-delivery-and-milestone-closeout
plan: 01
type: execute
wave: 1
depends_on: [11-03]
files_modified:
  - src/nixvibe/orchestrator/release_manifest.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_release_artifact_manifest.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Add release artifact manifest/checklist contract.

## Purpose
Make release handoff readiness explicit through deterministic artifact inventory and checklist evaluation.

## Output
- `release_artifact_manifest` contract builder
- Pipeline emission at `artifact_summary.release_artifact_manifest`
- Deterministic checklist for route/mode/release/audit requirements
- Regression tests for apply-ready and blocked release manifest paths
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/11-operator-policy-and-controls/11-03-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/release.py
@src/nixvibe/orchestrator/audittrail.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Release Manifest Contract
```gherkin
Given generated/proposed/written artifacts and release state
When release manifest is built
Then deterministic checklist and artifact inventory are emitted
```

## AC-2: Route and Mode Coverage
```gherkin
Given init and audit routes and apply/propose materialization states
When release manifest is built
Then required route/mode checklist items are evaluated deterministically
```

## AC-3: Pipeline Integration
```gherkin
Given orchestration output
When artifact summary is finalized
Then `release_artifact_manifest` is included in output contract
```

## AC-4: Regression Stability
```gherkin
Given release manifest integration
When targeted and full tests run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add release manifest helper</name>
  <files>src/nixvibe/orchestrator/release_manifest.py</files>
  <action>
    Implement deterministic release checklist logic with route/mode requirements and artifact inventory summary.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.release_manifest import build_release_artifact_manifest"</verify>
  <done>AC-1 and AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline output</name>
  <files>src/nixvibe/orchestrator/pipeline.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Emit `artifact_summary.release_artifact_manifest` and export helper.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests and docs</name>
  <files>tests/orchestrator/test_release_artifact_manifest.py, tests/orchestrator/test_acceptance_flows.py, README.md</files>
  <action>
    Add release manifest regression coverage and document checklist contract behavior.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/12-release-delivery-and-milestone-closeout/12-01-SUMMARY.md`
</output>
