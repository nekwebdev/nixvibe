---
phase: 12-release-delivery-and-milestone-closeout
plan: 02
type: execute
wave: 1
depends_on: [12-01]
files_modified:
  - src/nixvibe/orchestrator/release_check.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_release_check_command.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Add automated release-check command contract.

## Purpose
Standardize release-check execution behavior and status reporting with explicit skip/pending/pass/fail states.

## Output
- `release_check_command` contract builder
- Pipeline emission at `artifact_summary.release_check_command`
- Optional runner-based command execution integration
- Regression tests for skipped, pending, passed, and failed outcomes
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/12-release-delivery-and-milestone-closeout/12-01-SUMMARY.md

## Runtime Baseline
@scripts/release-check.sh
@src/nixvibe/orchestrator/release_manifest.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Release-Check Command Contract
```gherkin
Given release manifest readiness state
When release-check contract is generated
Then deterministic command status and next action are emitted
```

## AC-2: Execution Outcome Coverage
```gherkin
Given runner availability and execution result
When release-check contract is generated
Then statuses cover skipped/pending/passed/failed deterministically
```

## AC-3: Pipeline Integration
```gherkin
Given orchestration output
When artifact summary is finalized
Then `release_check_command` is included in output contract
```

## AC-4: Regression Stability
```gherkin
Given release-check integration
When targeted and full tests run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add release-check helper</name>
  <files>src/nixvibe/orchestrator/release_check.py</files>
  <action>
    Implement release-check command contract and optional runner execution behavior.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.release_check import build_release_check_command_contract"</verify>
  <done>AC-1 and AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline output</name>
  <files>src/nixvibe/orchestrator/pipeline.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Emit `artifact_summary.release_check_command` and export helper.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests and docs</name>
  <files>tests/orchestrator/test_release_check_command.py, tests/orchestrator/test_acceptance_flows.py, README.md</files>
  <action>
    Add release-check outcome regressions and document command contract behavior.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/12-release-delivery-and-milestone-closeout/12-02-SUMMARY.md`
</output>
