---
phase: 04-workspace-intake-and-reference-adaptation
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - src/nixvibe/orchestrator/types.py
  - src/nixvibe/orchestrator/workspace.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_reference_adaptation.py
  - README.md
autonomous: true
---

<objective>
## Goal
Integrate reference adaptation policy hints as first-class orchestration context.

## Purpose
Turn raw reference inspection signals into explicit adaptation guidance that downstream orchestration components can consume deterministically.

## Output
- Typed reference adaptation model on `RepoContext`
- Derived adaptation strategy (`preserve-and-extend` vs `bootstrap-from-reference-patterns`)
- Pipeline summary exposure of adaptation hints
- Regression coverage for adaptation derivation and summary contract
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/04-workspace-intake-and-reference-adaptation/04-01-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/types.py
@src/nixvibe/orchestrator/workspace.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Typed Reference Adaptation Model
```gherkin
Given optional reference profile data
When repo context is built
Then output includes a typed reference adaptation object with strategy and recommendations
```

## AC-2: Deterministic Adaptation Strategy
```gherkin
Given workspace and reference snapshots
When adaptation is derived
Then strategy is deterministic and reflects preserve/extend vs bootstrap intent
```

## AC-3: Adaptation Metadata in Pipeline Summary
```gherkin
Given orchestration with context profile
When pipeline returns
Then artifact summary includes reference adaptation metadata
```

## AC-4: Tests Lock Adaptation Behavior
```gherkin
Given adaptation derivation and summary contracts
When tests run
Then adaptation strategy, defaults, and policy-note behavior are validated
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Extend context types and builder for adaptation model</name>
  <files>src/nixvibe/orchestrator/types.py, src/nixvibe/orchestrator/workspace.py</files>
  <action>
    Add typed reference adaptation model and integrate derivation into context builder.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.workspace import derive_reference_adaptation"</verify>
  <done>AC-1 and AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Expose adaptation metadata in orchestration output</name>
  <files>src/nixvibe/orchestrator/pipeline.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Extend pipeline context-profile summary shape to include reference adaptation contract fields.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests and docs for adaptation integration</name>
  <files>tests/orchestrator/test_reference_adaptation.py, README.md</files>
  <action>
    Add deterministic tests for adaptation strategy selection, defaults, and summary exposure.
    Update runtime docs with Phase 4 adaptation slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-4 satisfied.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Policy ordering and write-mode semantics
- Artifact naming contracts

## SCOPE LIMITS
- No specialist dispatch wiring yet (reserved for 04-03)
- No Phase 5 runtime-execution features

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Reference adaptation dataclass exists and is populated via context builder
- [ ] Adaptation strategy logic is deterministic
- [ ] Pipeline summary includes adaptation metadata
- [ ] Full test suite passes
</verification>

<success_criteria>
- Reference adaptation is now explicit, typed, and consumable across runtime outputs
- Phase 4 progression is ready for 04-03 dispatch-context integration
</success_criteria>

<output>
After completion, create `.paul/phases/04-workspace-intake-and-reference-adaptation/04-02-SUMMARY.md`
</output>
