---
phase: 04-workspace-intake-and-reference-adaptation
plan: 01
type: execute
wave: 1
depends_on: [03-03]
files_modified:
  - src/nixvibe/orchestrator/types.py
  - src/nixvibe/orchestrator/workspace.py
  - src/nixvibe/orchestrator/router.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_workspace_intake.py
  - README.md
autonomous: true
---

<objective>
## Goal
Deliver the first Phase 4 slice for bounded workspace intake and reference-aware context profiling.

## Purpose
Allow orchestration to use optional filesystem snapshots (workspace and reference) to make safer route decisions without assuming unrestricted access.

## Output
- Typed workspace/reference profiling structures
- Bounded snapshot/context builder helper
- Routing behavior that can use profiled context when explicit flags are unknown
- Test coverage for profiling and route fallback behavior
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/03-validation-and-acceptance/03-03-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/types.py
@src/nixvibe/orchestrator/router.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Bounded Workspace Snapshot
```gherkin
Given a workspace path and entry limit
When context profiling runs
Then snapshot output is bounded, deterministic, and marks truncation when limit is hit
```

## AC-2: Optional Reference Inspection
```gherkin
Given an optional user-provided reference path
When profiling runs
Then output captures structure and validation-pattern hints
And references are adapted as hints, not copied artifacts
```

## AC-3: Routing Uses Profiled Context Hints
```gherkin
Given explicit repo flags are unknown
When a workspace snapshot is present
Then route selection uses profile-derived context before ambiguous fallback
```

## AC-4: Output Summary Includes Context Profile
```gherkin
Given profiled context is supplied to pipeline
When orchestration completes
Then artifact summary includes concise context-profile metadata
```

## AC-5: Tests Lock Behavior
```gherkin
Given workspace intake and routing integration
When tests run
Then bounded profiling and routing decisions are covered
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add typed workspace/reference profile models and builder</name>
  <files>src/nixvibe/orchestrator/types.py, src/nixvibe/orchestrator/workspace.py</files>
  <action>
    Introduce profile dataclasses and a bounded context builder that inspects workspace + optional reference path.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.workspace import build_repo_context"</verify>
  <done>AC-1 and AC-2 implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate profile hints into routing and pipeline summary</name>
  <files>src/nixvibe/orchestrator/router.py, src/nixvibe/orchestrator/pipeline.py, src/nixvibe/orchestrator/types.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Use profile hints when explicit context flags are unknown and add compact context-profile section in artifact summary.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-3 and AC-4 implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests and docs update</name>
  <files>tests/orchestrator/test_workspace_intake.py, README.md</files>
  <action>
    Add tests for bounded snapshots, optional reference hints, route behavior with profiled context, and summary metadata.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-5 satisfied with passing tests and docs.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Existing policy ordering or write-mode semantics
- Artifact naming contracts

## SCOPE LIMITS
- No full runtime specialist implementation yet (Phase 5)
- No UX adaptation overhaul yet (Phase 6)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Bounded profile builder works for workspace and optional reference path
- [ ] Routing uses profile hints when context flags are unknown
- [ ] Pipeline summary includes context profile metadata
- [ ] Full test suite passes
</verification>

<success_criteria>
- Phase 4 has a concrete intake baseline for real repo contexts
- Orchestration stays deterministic while using bounded optional filesystem insight
</success_criteria>

<output>
After completion, create `.paul/phases/04-workspace-intake-and-reference-adaptation/04-01-SUMMARY.md`
</output>
