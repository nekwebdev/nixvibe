---
phase: 04-workspace-intake-and-reference-adaptation
plan: 03
type: execute
wave: 1
depends_on: [04-02]
files_modified:
  - src/nixvibe/orchestrator/types.py
  - src/nixvibe/orchestrator/specialists.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_specialist_dispatch_context.py
  - README.md
autonomous: true
---

<objective>
## Goal
Complete Phase 4 by wiring intake-derived context into specialist dispatch execution.

## Purpose
Ensure specialist runners receive deterministic context contracts generated from route/mode decisions and workspace/reference intake signals.

## Output
- Typed specialist dispatch context model
- Pipeline wiring that attaches dispatch context to tasks before parallel execution
- Backward-compatible specialist runner invocation
- Dispatch metadata surfaced in pipeline summary output
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/04-workspace-intake-and-reference-adaptation/04-01-SUMMARY.md
@.paul/phases/04-workspace-intake-and-reference-adaptation/04-02-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/specialists.py
@src/nixvibe/orchestrator/pipeline.py
@src/nixvibe/orchestrator/types.py
</context>

<acceptance_criteria>

## AC-1: Dispatch Context Model Exists
```gherkin
Given route/mode decisions and repo context
When dispatch context is built
Then specialist dispatch receives a typed context object with intake-derived fields
```

## AC-2: Pipeline Wires Dispatch Context to Tasks
```gherkin
Given specialist tasks and built context
When pipeline executes specialists
Then tasks run with attached dispatch context and preserve deterministic ordering
```

## AC-3: Backward Compatibility for Existing Runners
```gherkin
Given zero-argument specialist runners
When dispatch context is attached
Then runners still execute successfully without requiring signature changes
```

## AC-4: Dispatch Metadata in Output Summary
```gherkin
Given orchestrator execution
When output is produced
Then artifact summary includes specialist dispatch metadata
```

## AC-5: Tests Lock Dispatch Wiring Behavior
```gherkin
Given context-aware and legacy runners
When tests run
Then dispatch context behavior and compatibility remain covered
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add dispatch context type and builder helpers</name>
  <files>src/nixvibe/orchestrator/types.py, src/nixvibe/orchestrator/specialists.py</files>
  <action>
    Add typed dispatch context contract and helper functions to build and attach it to specialist tasks.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.specialists import build_dispatch_context"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire dispatch context in pipeline and summary</name>
  <files>src/nixvibe/orchestrator/pipeline.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Build dispatch context from route/mode/context, attach to tasks, execute specialists, and include dispatch metadata in summary.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 and AC-4 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add compatibility tests and docs update</name>
  <files>tests/orchestrator/test_specialist_dispatch_context.py, README.md</files>
  <action>
    Add tests for context-aware runners, legacy runner compatibility, and dispatch summary metadata.
    Update docs with Phase 4 dispatch slice behavior.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-5 satisfied.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Conflict policy ordering
- Write-mode semantics
- Artifact naming conventions

## SCOPE LIMITS
- No Phase 5 runtime execution logic
- No Phase 6 UX/guardrail changes

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Typed dispatch context integrated end-to-end
- [ ] Context-aware and zero-arg runners both pass
- [ ] Dispatch metadata appears in artifact summary
- [ ] Full test suite passes
</verification>

<success_criteria>
- Intake-driven specialist dispatch context wiring is complete for Phase 4
- Project is ready to begin Phase 5 planning
</success_criteria>

<output>
After completion, create `.paul/phases/04-workspace-intake-and-reference-adaptation/04-03-SUMMARY.md`
</output>
