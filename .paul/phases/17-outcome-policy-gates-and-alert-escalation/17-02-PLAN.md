---
phase: 17-outcome-policy-gates-and-alert-escalation
plan: 02
type: execute
wave: 1
depends_on: [17-01]
files_modified:
  - src/nixvibe/orchestrator/alert_policy_gate.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_alert_policy_gate.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Integrate deterministic alert-aware policy gates for apply and release behavior.

## Purpose
Advance phase 17 by linking outcome alerts to explicit policy gate contracts that can guide apply/release decisions in subsequent escalation flows.

## Output
- `alert_policy_gate` contract (`alert-policy-gate/v1`)
- Deterministic gate outcomes for apply/release paths
- Pipeline integration at `artifact_summary.alert_policy_gate`
- Regression tests for allow/warn/block policy gate paths
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/17-outcome-policy-gates-and-alert-escalation/17-01-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/outcome_alert.py
@src/nixvibe/orchestrator/release.py
@src/nixvibe/orchestrator/escalation.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Alert Policy Gate Contract
```gherkin
Given outcome alert and release/apply context
When alert policy gate evaluates
Then deterministic apply/release gate decisions and next-action metadata are emitted
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration output assembly
When artifact summary is finalized
Then `artifact_summary.alert_policy_gate` is always present
```

## AC-3: Gate Severity Paths
```gherkin
Given nominal, warning, critical, and blocked alert contexts
When gate policy is evaluated
Then allow, allow-with-warning, and deny outcomes are deterministic
```

## AC-4: Regression Stability
```gherkin
Given alert policy gate integration
When targeted and full suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add alert policy gate helper and exports</name>
  <files>src/nixvibe/orchestrator/alert_policy_gate.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic alert-aware policy gate contract that maps alert severity to apply/release gate outcomes.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.alert_policy_gate import build_alert_policy_gate"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire alert policy gate output in pipeline</name>
  <files>src/nixvibe/orchestrator/pipeline.py, tests/orchestrator/test_acceptance_flows.py</files>
  <action>
    Emit `artifact_summary.alert_policy_gate` from outcome alert + release/apply context so downstream escalation paths consume explicit gate metadata.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add gate-path tests and docs</name>
  <files>tests/orchestrator/test_alert_policy_gate.py, README.md</files>
  <action>
    Add deterministic allow/warn/block gate tests and document phase17 alert-policy gate slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/17-outcome-policy-gates-and-alert-escalation/17-02-SUMMARY.md`
</output>
