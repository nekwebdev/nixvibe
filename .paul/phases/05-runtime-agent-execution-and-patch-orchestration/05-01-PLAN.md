---
phase: 05-runtime-agent-execution-and-patch-orchestration
plan: 01
type: execute
wave: 1
depends_on: [04-03]
files_modified:
  - src/nixvibe/orchestrator/types.py
  - src/nixvibe/orchestrator/runtime.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_runtime_contract.py
  - README.md
autonomous: true
---

<objective>
## Goal
Establish a concrete runtime specialist execution contract for Phase 5.

## Purpose
Move from ad-hoc externally supplied specialist task lists toward a typed, deterministic runtime contract that can build executable specialist tasks from route-aware definitions and handlers.

## Output
- Typed runtime specialist contract model
- Runtime planner that maps contract + handlers to executable specialist tasks
- Pipeline support for contract-driven specialist execution
- Tests for deterministic ordering, required/optional role handling, and pipeline integration
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/04-workspace-intake-and-reference-adaptation/04-03-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/types.py
@src/nixvibe/orchestrator/specialists.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Typed Runtime Specialist Contract
```gherkin
Given runtime orchestration requirements
When contract models are defined
Then runtime specialist roles/specs/contracts are explicit and typed
```

## AC-2: Deterministic Task Planning from Contract
```gherkin
Given a runtime contract and handler registry
When tasks are planned
Then executable specialist tasks are generated in deterministic contract order
```

## AC-3: Required/Optional Role Enforcement
```gherkin
Given missing specialist handlers
When a required role is missing
Then planning fails with explicit contract error
And when optional role is missing, planning continues safely
```

## AC-4: Pipeline Contract Execution Path
```gherkin
Given no explicit specialist tasks
When a runtime contract and handlers are provided
Then pipeline executes specialists from the contract path and returns merged output
```

## AC-5: Tests Lock Runtime Contract Behavior
```gherkin
Given runtime contract planning/execution
When tests run
Then ordering, role enforcement, and pipeline integration behavior are covered
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add runtime contract types and planner module</name>
  <files>src/nixvibe/orchestrator/types.py, src/nixvibe/orchestrator/runtime.py</files>
  <action>
    Add typed runtime contract structures and deterministic planner that materializes SpecialistTask entries from contract specs and handler registry.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.runtime import plan_runtime_specialists"</verify>
  <done>AC-1 and AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline contract execution path</name>
  <files>src/nixvibe/orchestrator/pipeline.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Add optional pipeline path to build specialist tasks from runtime contract + handlers when explicit task list is empty.
    Keep existing explicit task behavior backward-compatible.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-4 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests and docs for runtime contract path</name>
  <files>tests/orchestrator/test_runtime_contract.py, README.md</files>
  <action>
    Add tests for deterministic planning, required/optional role handling, and pipeline contract-driven execution.
    Update runtime docs for 05-01 contract behavior.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-5 satisfied.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Conflict policy ordering
- Existing write-mode semantics
- Existing artifact naming contracts

## SCOPE LIMITS
- No patch orchestration implementation yet (05-02)
- No validation checkpoint enhancements yet (05-03)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Runtime contract model and planner exist
- [ ] Required vs optional role behavior enforced
- [ ] Pipeline can execute from contract+handlers path
- [ ] Full test suite passes
</verification>

<success_criteria>
- Phase 5 runtime execution has a concrete typed contract baseline
- System is ready for 05-02 patch orchestration integration
</success_criteria>

<output>
After completion, create `.paul/phases/05-runtime-agent-execution-and-patch-orchestration/05-01-SUMMARY.md`
</output>
