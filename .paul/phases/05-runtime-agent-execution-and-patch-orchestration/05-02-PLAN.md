---
phase: 05-runtime-agent-execution-and-patch-orchestration
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - src/nixvibe/orchestrator/patches.py
  - src/nixvibe/orchestrator/merge.py
  - src/nixvibe/orchestrator/artifacts.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_patch_orchestration_runtime.py
  - README.md
autonomous: true
---

<objective>
## Goal
Integrate deterministic patch orchestration into runtime specialist outputs.

## Purpose
Turn aggregated specialist patch hints into a normalized, ordered patch plan contract that remains stable across runtime and explicit task execution paths.

## Output
- Shared patch orchestration module (normalize/orchestrate/summary)
- Merge integration that emits `patch_orchestration` summary
- Artifact flow alignment using shared normalization logic
- Runtime-focused tests for deterministic patch ordering and contract metadata
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/05-runtime-agent-execution-and-patch-orchestration/05-01-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/merge.py
@src/nixvibe/orchestrator/artifacts.py
@src/nixvibe/orchestrator/runtime.py
</context>

<acceptance_criteria>

## AC-1: Deterministic Runtime Patch Orchestration
```gherkin
Given patch outputs from multiple specialists
When merge orchestrates patch proposals
Then patch plan order and normalized paths are deterministic
```

## AC-2: Patch Contract Metadata in Summary
```gherkin
Given orchestrated patch proposals
When pipeline output is produced
Then artifact summary includes patch orchestration metadata (count/paths/ids/source agents)
```

## AC-3: Shared Patch Path Normalization
```gherkin
Given patch path normalization in runtime and artifact stages
When normalization runs
Then shared logic is used and behavior stays consistent
```

## AC-4: Runtime Path Coverage
```gherkin
Given runtime contract execution path
When specialists emit patches
Then orchestrated patch metadata and generated patch files align
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add patch orchestration module and integrate merge</name>
  <files>src/nixvibe/orchestrator/patches.py, src/nixvibe/orchestrator/merge.py</files>
  <action>
    Implement normalized patch orchestration helpers and integrate them into artifact merge summary.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.patches import orchestrate_patch_proposals"</verify>
  <done>AC-1 and AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Align artifact normalization and exports</name>
  <files>src/nixvibe/orchestrator/artifacts.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Reuse shared normalization helper in artifact generation and export patch helpers.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator import normalize_patch_path"</verify>
  <done>AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add runtime patch orchestration tests and docs</name>
  <files>tests/orchestrator/test_patch_orchestration_runtime.py, README.md</files>
  <action>
    Add runtime-focused tests for deterministic patch plan behavior and summary contract integration.
    Update docs for Phase 5 patch orchestration slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/05-runtime-agent-execution-and-patch-orchestration/05-02-SUMMARY.md`
</output>
