---
phase: 05-runtime-agent-execution-and-patch-orchestration
plan: 03
type: execute
wave: 1
depends_on: [05-02]
files_modified:
  - src/nixvibe/orchestrator/pipeline.py
  - tests/orchestrator/test_pipeline_validation_gating.py
  - tests/orchestrator/test_runtime_contract.py
  - README.md
autonomous: true
---

<objective>
## Goal
Add explicit validation checkpoints to apply-mode write flows.

## Purpose
Ensure validation behavior is observable and deterministic across pre-write and post-write stages in runtime orchestration.

## Output
- Pre-write validation gate before apply writes
- Post-write validation checkpoint after successful apply writes
- Artifact summary metadata exposing checkpoint sequence and final checkpoint status
- Test coverage for explicit-task and runtime-contract execution paths
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/05-runtime-agent-execution-and-patch-orchestration/05-02-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/pipeline.py
@src/nixvibe/orchestrator/validation.py
</context>

<acceptance_criteria>

## AC-1: Pre-Write Validation Gate
```gherkin
Given apply mode is requested
When pre-write validation fails
Then writes are blocked and mode downgrades to propose
```

## AC-2: Post-Write Validation Checkpoint
```gherkin
Given apply mode passes pre-write validation
When artifact writes complete
Then post-write validation executes and is recorded
```

## AC-3: Checkpoint Summary Contract
```gherkin
Given validation checkpoints execute
When artifact summary is built
Then validation metadata includes checkpoint sequence and final checkpoint state
```

## AC-4: Runtime Contract Path Coverage
```gherkin
Given runtime contract execution path in apply mode
When validation runs
Then pre-write and post-write checkpoints are executed deterministically
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Implement checkpointed apply validation flow</name>
  <files>src/nixvibe/orchestrator/pipeline.py</files>
  <action>
    Add pre-write and post-write validation checkpoints, preserve mode-gating safety, and expose checkpoint metadata.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-1, AC-2, and AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Extend validation tests</name>
  <files>tests/orchestrator/test_pipeline_validation_gating.py, tests/orchestrator/test_runtime_contract.py</files>
  <action>
    Add assertions for checkpoint sequence/final state and runtime-contract apply-path checkpoint execution.
  </action>
  <verify>python -m unittest tests.orchestrator.test_pipeline_validation_gating tests.orchestrator.test_runtime_contract -v</verify>
  <done>AC-4 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs and phase artifacts</name>
  <files>README.md, .paul/phases/05-runtime-agent-execution-and-patch-orchestration/05-03-SUMMARY.md</files>
  <action>
    Document checkpointed validation behavior and record plan completion.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>Phase documentation is synchronized.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/05-runtime-agent-execution-and-patch-orchestration/05-03-SUMMARY.md`
</output>
