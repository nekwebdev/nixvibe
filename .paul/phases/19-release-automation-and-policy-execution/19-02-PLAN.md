---
phase: 19-release-automation-and-policy-execution
plan: 02
type: execute
wave: 1
depends_on: [19-01]
files_modified:
  - src/nixvibe/orchestrator/release_policy_execution.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_release_policy_execution.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Integrate release policy execution contract into the v0.7 release flow.

## Purpose
Convert release execution gate and override workflow outcomes into deterministic execution policy decisions.

## Output
- `release_policy_execution` contract (`release-policy-execution/v1`)
- Pipeline integration at `artifact_summary.release_policy_execution`
- Deterministic policy execution decisions (`automated`, `manual-ack`, `blocked`)
- Regression tests for policy execution paths
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/19-release-automation-and-policy-execution/19-01-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/release_execution_gate.py
@src/nixvibe/orchestrator/override.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Release Policy Execution Contract
```gherkin
Given release execution gate and override workflow outcomes
When release policy execution evaluates
Then deterministic automated/manual/blocking policy metadata is emitted
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration artifact assembly
When artifact summary is finalized
Then `artifact_summary.release_policy_execution` is always present
```

## AC-3: Policy Execution Paths
```gherkin
Given allow, hold, deny, and override-denied contexts
When release policy execution evaluates
Then decision and blockers remain deterministic
```

## AC-4: Regression Stability
```gherkin
Given release policy execution integration
When targeted and full suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add release policy execution helper and exports</name>
  <files>src/nixvibe/orchestrator/release_policy_execution.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic release policy execution contract mapping release gate and override workflow outcomes to automated/manual/blocking execution decisions.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.release_policy_execution import build_release_policy_execution"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire release policy execution in pipeline</name>
  <files>src/nixvibe/orchestrator/pipeline.py, tests/orchestrator/test_acceptance_flows.py</files>
  <action>
    Emit `artifact_summary.release_policy_execution` using release execution gate, override workflow, and release-check context.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add policy execution tests and docs</name>
  <files>tests/orchestrator/test_release_policy_execution.py, README.md</files>
  <action>
    Add deterministic policy execution path tests and document the phase19 policy execution integration slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/19-release-automation-and-policy-execution/19-02-SUMMARY.md`
</output>
