---
phase: 19-release-automation-and-policy-execution
plan: 01
type: execute
wave: 1
depends_on: [18-03]
files_modified:
  - src/nixvibe/orchestrator/release_execution_gate.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_release_execution_gate.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Introduce automated release execution gate contract for v0.7.

## Purpose
Start phase 19 by defining deterministic release execution eligibility from v0.6 readiness summary and release-check outcomes.

## Output
- `release_execution_gate` contract (`release-execution-gate/v1`)
- Pipeline integration at `artifact_summary.release_execution_gate`
- Deterministic execution decisions (`allow`, `hold`, `deny`)
- Regression tests for gate paths
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/18-release-candidate-evidence-and-v0.6-closeout/18-03-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/v06_readiness_summary.py
@src/nixvibe/orchestrator/release_check.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Release Execution Gate Contract
```gherkin
Given v0.6 readiness summary and release-check status
When release execution gate evaluates
Then deterministic allow/hold/deny decision metadata is emitted
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration artifact assembly
When artifact summary is finalized
Then `artifact_summary.release_execution_gate` is always present
```

## AC-3: Gate Decision Paths
```gherkin
Given ready, hold, and blocked release contexts
When release execution gate evaluates
Then execution decision and blockers are deterministic
```

## AC-4: Regression Stability
```gherkin
Given release execution gate integration
When targeted and full suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add release execution gate helper and exports</name>
  <files>src/nixvibe/orchestrator/release_execution_gate.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic release execution gate contract mapping readiness summary and release-check status to allow/hold/deny decisions.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.release_execution_gate import build_release_execution_gate"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire release execution gate in pipeline</name>
  <files>src/nixvibe/orchestrator/pipeline.py, tests/orchestrator/test_acceptance_flows.py</files>
  <action>
    Emit `artifact_summary.release_execution_gate` using readiness summary and release-check output for automated release decisions.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add execution-gate tests and docs</name>
  <files>tests/orchestrator/test_release_execution_gate.py, README.md</files>
  <action>
    Add deterministic allow/hold/deny release execution gate tests and document phase19 execution-gate slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/19-release-automation-and-policy-execution/19-01-SUMMARY.md`
</output>
