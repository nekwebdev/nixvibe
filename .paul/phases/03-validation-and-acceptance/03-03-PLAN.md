---
phase: 03-validation-and-acceptance
plan: 03
type: execute
wave: 1
depends_on: [03-02]
files_modified:
  - src/nixvibe/orchestrator/artifacts.py
  - tests/orchestrator/test_patch_hygiene.py
  - scripts/release-check.sh
  - README.md
  - .paul/ROADMAP.md
  - .paul/STATE.md
  - .paul/paul.json
autonomous: true
---

<objective>
## Goal
Complete Phase 3 by enforcing patch artifact hygiene and adding a deterministic release-check path.

## Purpose
Make audit patch outputs safer and more predictable while providing one command path to verify release readiness.

## Output
- Patch path normalization that guarantees `patches/*.patch` outputs
- Tests that validate naming/path hygiene behavior
- A release-check script that runs required local verification steps
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/03-validation-and-acceptance/03-01-SUMMARY.md
@.paul/phases/03-validation-and-acceptance/03-02-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/artifacts.py
@tests/orchestrator/test_artifact_pipeline.py
</context>

<acceptance_criteria>

## AC-1: Patch Paths are Always Hygienic
```gherkin
Given audit patch metadata from specialists
When artifacts are generated
Then patch outputs are constrained to `patches/`
And names are normalized to `.patch` files with deterministic numbering
```

## AC-2: Invalid or Unsafe Patch Inputs are Adapted
```gherkin
Given malformed, absolute, or traversal patch path inputs
When normalization runs
Then outputs are adapted to safe `patches/*.patch` names
```

## AC-3: Release Check Command Path Exists
```gherkin
Given repository release prep
When release check script runs
Then it executes test suite and, when flake exists, `nix flake check` and `nix fmt`
```

## AC-4: Test Coverage Locks Patch Hygiene
```gherkin
Given patch normalization behavior
When orchestrator tests run
Then hygiene edge cases and deterministic output are covered
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Implement patch-path normalization in artifact generation</name>
  <files>src/nixvibe/orchestrator/artifacts.py</files>
  <action>
    Add path sanitization/normalization so patch artifacts are always emitted under `patches/` with `.patch` extension and deterministic numbering.
    Preserve stable valid inputs when already compliant.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.artifacts import generate_artifact_bundle"</verify>
  <done>AC-1 and AC-2 implementation complete.</done>
</task>

<task type="auto">
  <name>Task 2: Add patch hygiene tests</name>
  <files>tests/orchestrator/test_patch_hygiene.py</files>
  <action>
    Add tests for:
    - compliant patch paths
    - non-patches directory paths
    - traversal/absolute path inputs
    - duplicate input handling
    Ensure outputs are deterministic and safe.
  </action>
  <verify>python -m unittest tests.orchestrator.test_patch_hygiene -v</verify>
  <done>AC-4 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add release-check script and docs updates</name>
  <files>scripts/release-check.sh, README.md</files>
  <action>
    Add a release-check helper script that runs:
    - `python -m unittest discover -s tests -p 'test_*.py' -v`
    - and, if `flake.nix` exists, `nix flake check` + `nix fmt`
    Update README with script usage.
  </action>
  <verify>bash scripts/release-check.sh</verify>
  <done>AC-3 satisfied.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Policy ordering or write-mode behavior
- Existing artifact file contracts outside patch hygiene normalization

## SCOPE LIMITS
- No GUI/client work
- No expansion beyond release checks + patch hygiene

</boundaries>

<verification>
Before declaring plan complete:
- [ ] Patch outputs always land under `patches/` and end in `.patch`
- [ ] Unsafe patch path inputs are normalized safely
- [ ] Patch hygiene tests pass
- [ ] Full test suite passes
</verification>

<success_criteria>
- Phase 3 closes with validation, acceptance, and patch/release hygiene complete
- v0.1 runtime contracts are fully enforced in code and tests
</success_criteria>

<output>
After completion, create `.paul/phases/03-validation-and-acceptance/03-03-SUMMARY.md`
</output>
