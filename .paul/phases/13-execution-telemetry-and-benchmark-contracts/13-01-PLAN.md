---
phase: 13-execution-telemetry-and-benchmark-contracts
plan: 01
type: execute
wave: 1
depends_on: [12-03]
files_modified:
  - src/nixvibe/orchestrator/telemetry.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/manifest.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_run_telemetry_contract.py
  - tests/orchestrator/test_operator_run_manifest.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Add deterministic run telemetry contract with stage timing metadata for orchestration runs.

## Purpose
Start v0.5 outcome measurement by exposing stable per-run timing and throughput counters for operators and benchmark tooling.

## Output
- `run_telemetry` contract builder for deterministic timing/counter output
- Pipeline timing capture and `artifact_summary.run_telemetry` integration
- Operator run-manifest timing section sourced from telemetry contract
- Regression tests for telemetry contract and manifest timing presence
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/12-release-delivery-and-milestone-closeout/12-03-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/pipeline.py
@src/nixvibe/orchestrator/manifest.py
@tests/orchestrator/test_operator_run_manifest.py
</context>

<acceptance_criteria>

## AC-1: Telemetry Contract
```gherkin
Given orchestration execution stage timings
When telemetry output is built
Then deterministic counters and timing fields are emitted via `run_telemetry`
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration pipeline execution
When artifact summary is finalized
Then `artifact_summary.run_telemetry` is always present with stable schema
```

## AC-3: Operator Timing Surface
```gherkin
Given operator run manifest output
When telemetry is present
Then manifest includes normalized timing metadata for total/specialist/validation/materialization stages
```

## AC-4: Regression Stability
```gherkin
Given run telemetry integration
When targeted and full test suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add run telemetry contract helper</name>
  <files>src/nixvibe/orchestrator/telemetry.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic telemetry contract builder with stage timing and throughput counters.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.telemetry import build_run_telemetry"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline + manifest timing integration</name>
  <files>src/nixvibe/orchestrator/pipeline.py, src/nixvibe/orchestrator/manifest.py</files>
  <action>
    Capture deterministic stage timings in pipeline, emit `artifact_summary.run_telemetry`, and surface timing metadata in run manifest.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 and AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests + docs and verify full suite</name>
  <files>tests/orchestrator/test_run_telemetry_contract.py, tests/orchestrator/test_operator_run_manifest.py, tests/orchestrator/test_acceptance_flows.py, README.md</files>
  <action>
    Add deterministic telemetry tests, manifest timing assertions, and documentation for Phase 13 telemetry slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/13-execution-telemetry-and-benchmark-contracts/13-01-SUMMARY.md`
</output>
