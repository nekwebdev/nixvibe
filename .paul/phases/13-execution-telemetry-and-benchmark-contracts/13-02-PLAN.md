---
phase: 13-execution-telemetry-and-benchmark-contracts
plan: 02
type: execute
wave: 1
depends_on: [13-01]
files_modified:
  - src/nixvibe/orchestrator/benchmark.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_benchmark_baseline_report.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Add deterministic benchmark baseline report contract derived from run telemetry and readiness signals.

## Purpose
Provide machine-readable baseline measurement primitives needed for benchmark harness and milestone outcome tracking.

## Output
- `benchmark_baseline_report` contract helper (`benchmark-baseline-report/v1`)
- Pipeline output integration at `artifact_summary.benchmark_baseline_report`
- Deterministic baseline eligibility gates and performance tier mapping
- Regression tests for apply-success, propose, and blocked safety paths
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/13-execution-telemetry-and-benchmark-contracts/13-01-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/telemetry.py
@src/nixvibe/orchestrator/pipeline.py
@src/nixvibe/orchestrator/release.py
</context>

<acceptance_criteria>

## AC-1: Benchmark Baseline Contract
```gherkin
Given run telemetry and reliability/readiness metadata
When benchmark baseline report is built
Then deterministic baseline eligibility, checks, and timing summary are emitted
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration output assembly
When artifact summary is finalized
Then `artifact_summary.benchmark_baseline_report` is always present
```

## AC-3: Deterministic Path Coverage
```gherkin
Given apply-success, propose-only, and safety-blocked runs
When baseline report is evaluated
Then benchmark eligibility and failed checks remain deterministic
```

## AC-4: Regression Stability
```gherkin
Given benchmark baseline integration
When targeted and full test suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add benchmark baseline report helper</name>
  <files>src/nixvibe/orchestrator/benchmark.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic benchmark baseline contract using telemetry, mode/readiness gates, and failure classification.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.benchmark import build_benchmark_baseline_report"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline benchmark output</name>
  <files>src/nixvibe/orchestrator/pipeline.py</files>
  <action>
    Emit `artifact_summary.benchmark_baseline_report` after telemetry/reliability/readiness contracts are available.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests/docs and verify suite</name>
  <files>tests/orchestrator/test_benchmark_baseline_report.py, tests/orchestrator/test_acceptance_flows.py, README.md</files>
  <action>
    Add deterministic path tests for baseline eligibility checks and document the Phase 13 benchmark baseline slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/13-execution-telemetry-and-benchmark-contracts/13-02-SUMMARY.md`
</output>
