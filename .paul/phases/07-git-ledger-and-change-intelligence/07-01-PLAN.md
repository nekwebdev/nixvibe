---
phase: 07-git-ledger-and-change-intelligence
plan: 01
type: execute
wave: 1
depends_on: [06-03]
files_modified:
  - src/nixvibe/orchestrator/ledger.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_git_ledger.py
  - README.md
autonomous: true
---

<objective>
## Goal
Establish a Git ledger baseline for orchestration runtime context.

## Purpose
Enable nixvibe to treat Git workspace state as internal operational memory so apply/propose workflows can use consistent change visibility without exposing raw VCS complexity to users.

## Output
- Git ledger inspection helper for workspace repos
- Pipeline summary integration under `artifact_summary.ledger`
- Ledger contract tests for non-repo and repo-dirty scenarios
- Documentation update for Phase 7 ledger slice
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/06-guidance-ux-and-safety-guardrails/06-03-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/pipeline.py
@src/nixvibe/orchestrator/guidance.py
</context>

<acceptance_criteria>

## AC-1: Non-Repo Safety Contract
```gherkin
Given a workspace outside git
When ledger inspection runs
Then summary reports `available=false` with explicit reason
```

## AC-2: Repo Ledger Contract
```gherkin
Given a git workspace with pending changes
When pipeline runs
Then artifact summary includes branch/head/dirty counters and changed paths
```

## AC-3: Pipeline Integration Stability
```gherkin
Given existing orchestration flows
When ledger metadata is added
Then existing behavior remains unchanged and tests stay green
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add ledger inspection module</name>
  <files>src/nixvibe/orchestrator/ledger.py</files>
  <action>
    Implement git workspace ledger inspection and normalized summary contract.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.ledger import inspect_git_ledger"</verify>
  <done>AC-1 and AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate ledger summary into pipeline</name>
  <files>src/nixvibe/orchestrator/pipeline.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Surface ledger output in artifact summary and export helper from orchestrator package.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator import inspect_git_ledger"</verify>
  <done>Ledger contract available to runtime consumers.</done>
</task>

<task type="auto">
  <name>Task 3: Add ledger tests and docs</name>
  <files>tests/orchestrator/test_git_ledger.py, README.md</files>
  <action>
    Add non-repo and dirty-repo ledger tests; document Phase 7 ledger behavior.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/07-git-ledger-and-change-intelligence/07-01-SUMMARY.md`
</output>
