---
phase: 07-git-ledger-and-change-intelligence
plan: 03
type: execute
wave: 1
depends_on: [07-02]
files_modified:
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/guidance.py
  - tests/orchestrator/test_git_ledger.py
  - README.md
autonomous: true
---

<objective>
## Goal
Tune guidance and next-action behavior using ledger change-intelligence signals.

## Purpose
Convert ledger drift/classification data into user-facing orchestration guidance so propose/apply flows provide clearer operational direction.

## Output
- Ledger-aware next-action tuning in pipeline
- Ledger-aware guidance fields and action hint contract
- Regression tests for propose/apply ledger-aware guidance behavior
- Documentation update for ledger-aware guidance slice
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/07-git-ledger-and-change-intelligence/07-02-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/pipeline.py
@src/nixvibe/orchestrator/guidance.py
@src/nixvibe/orchestrator/ledger.py
</context>

<acceptance_criteria>

## AC-1: Propose Drift Next-Action Tuning
```gherkin
Given propose mode with ledger drift
When pipeline resolves immediate next action
Then next action explicitly requests drift reconciliation before apply
```

## AC-2: Apply Ledger Checkpoint Tuning
```gherkin
Given apply mode with ledger-detected workspace changes
When pipeline resolves immediate next action
Then next action explicitly requests ledger checkpoint review
```

## AC-3: Ledger-Aware Guidance Contract
```gherkin
Given orchestration output with ledger summary
When guidance contract is emitted
Then guidance includes ledger classification/drift fields and action hint
```

## AC-4: Regression Stability
```gherkin
Given the full orchestration test suite
When ledger-aware tuning is integrated
Then all tests remain green
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add ledger-aware next-action tuning</name>
  <files>src/nixvibe/orchestrator/pipeline.py</files>
  <action>
    Tune propose/apply next-action output using ledger drift and change classification signals.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-1 and AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Extend guidance contract with ledger action hints</name>
  <files>src/nixvibe/orchestrator/guidance.py</files>
  <action>
    Add ledger-aware guidance fields and action-hint mapping for propose/apply/advice modes.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.guidance import build_guidance_summary"</verify>
  <done>AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests and docs</name>
  <files>tests/orchestrator/test_git_ledger.py, README.md</files>
  <action>
    Add regression assertions for ledger-aware next-action/guidance behavior and update docs.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/07-git-ledger-and-change-intelligence/07-03-SUMMARY.md`
</output>
