---
phase: 07-git-ledger-and-change-intelligence
plan: 02
type: execute
wave: 1
depends_on: [07-01]
files_modified:
  - src/nixvibe/orchestrator/ledger.py
  - tests/orchestrator/test_git_ledger.py
  - README.md
autonomous: true
---

<objective>
## Goal
Add deterministic ledger change classification and drift signals.

## Purpose
Turn raw git status counters into explicit operational intelligence that downstream orchestration and guidance layers can consume safely.

## Output
- Change classification contract (`clean`, `staged-only`, `unstaged-only`, `untracked-only`, mixed forms)
- Drift signals (`drift_detected`, `drift_reasons`, `drift_severity`)
- Ledger tests for clean and dirty repository scenarios
- Documentation update for classification/drift behavior
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/07-git-ledger-and-change-intelligence/07-01-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/ledger.py
@tests/orchestrator/test_git_ledger.py
</context>

<acceptance_criteria>

## AC-1: Change Classification Contract
```gherkin
Given git status counters
When ledger summary is produced
Then summary includes deterministic change classification values
```

## AC-2: Drift Signal Contract
```gherkin
Given repository drift scenarios
When ledger summary is produced
Then drift detection, reasons, and severity are explicit
```

## AC-3: Regression Stability
```gherkin
Given existing pipeline behavior
When change classification and drift signals are added
Then full suite remains green
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Extend ledger change-intelligence model</name>
  <files>src/nixvibe/orchestrator/ledger.py</files>
  <action>
    Add deterministic change-class and drift-signal fields to ledger summary.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.ledger import inspect_git_ledger"</verify>
  <done>AC-1 and AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ledger regression coverage</name>
  <files>tests/orchestrator/test_git_ledger.py</files>
  <action>
    Add clean-state and dirty-state assertions for classification/drift signals.
  </action>
  <verify>python -m unittest tests.orchestrator.test_git_ledger -v</verify>
  <done>Ledger behavior regression-locked.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs and verify suite</name>
  <files>README.md</files>
  <action>
    Document new classification and drift fields and run full regression suite.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/07-git-ledger-and-change-intelligence/07-02-SUMMARY.md`
</output>
