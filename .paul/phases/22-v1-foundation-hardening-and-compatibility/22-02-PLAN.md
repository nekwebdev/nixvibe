---
phase: 22-v1-foundation-hardening-and-compatibility
plan: 02
type: execute
wave: 1
depends_on: [22-01]
files_modified:
  - src/nixvibe/orchestrator/migration_safety_policy.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_migration_safety_policy.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Integrate migration-safety policy contract for v1 compatibility flow.

## Purpose
Convert compatibility baseline and safety/override signals into deterministic allow/review/block migration policy decisions for v1 transition work.

## Output
- `migration_safety_policy` contract (`migration-safety-policy/v1`)
- Pipeline integration at `artifact_summary.migration_safety_policy`
- Deterministic migration policy decision (`allow`, `review`, `block`)
- Regression tests for migration policy helper and pipeline surface
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/22-v1-foundation-hardening-and-compatibility/22-01-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/v10_compatibility_baseline.py
@src/nixvibe/orchestrator/escalation.py
@src/nixvibe/orchestrator/override.py
@src/nixvibe/orchestrator/release_policy_execution.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Migration Safety Policy Contract
```gherkin
Given compatibility, apply safety, override, and release policy outcomes
When migration safety policy evaluates
Then deterministic allow/review/block migration metadata is emitted
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration artifact assembly
When artifact summary finalizes
Then `artifact_summary.migration_safety_policy` is always present
```

## AC-3: Migration Decision Determinism
```gherkin
Given safe, guarded, and blocked transition contexts
When migration safety policy evaluates
Then policy decision and blockers remain deterministic
```

## AC-4: Regression Stability
```gherkin
Given migration safety integration
When targeted and full suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add migration safety policy helper and exports</name>
  <files>src/nixvibe/orchestrator/migration_safety_policy.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic migration safety policy contract aggregating compatibility baseline, apply safety escalation, override workflow, and release policy signals.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.migration_safety_policy import build_migration_safety_policy"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire migration safety policy in pipeline</name>
  <files>src/nixvibe/orchestrator/pipeline.py, tests/orchestrator/test_acceptance_flows.py</files>
  <action>
    Emit `artifact_summary.migration_safety_policy` from compatibility/safety/override/release outputs.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add migration safety tests and docs</name>
  <files>tests/orchestrator/test_migration_safety_policy.py, README.md</files>
  <action>
    Add deterministic migration decision tests and document phase22 migration safety policy slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/22-v1-foundation-hardening-and-compatibility/22-02-SUMMARY.md`
</output>
