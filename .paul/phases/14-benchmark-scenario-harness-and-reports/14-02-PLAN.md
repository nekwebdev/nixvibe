---
phase: 14-benchmark-scenario-harness-and-reports
plan: 02
type: execute
wave: 1
depends_on: [14-01]
files_modified:
  - src/nixvibe/orchestrator/benchmark_runner.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_benchmark_runner_report.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Add deterministic benchmark runner report emitter for scenario execution planning.

## Purpose
Advance phase 14 from catalog-only to executable runner planning with machine-readable run order and command contracts.

## Output
- `benchmark_runner_report` contract helper (`benchmark-runner-report/v1`)
- Deterministic run plan from recommended scenario ids
- Pipeline integration at `artifact_summary.benchmark_runner_report`
- Regression tests for baseline, recovery, and regression run plans
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/14-benchmark-scenario-harness-and-reports/14-01-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/benchmark_scenarios.py
@src/nixvibe/orchestrator/benchmark.py
@src/nixvibe/orchestrator/telemetry_regression.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Benchmark Runner Report Contract
```gherkin
Given scenario catalog and benchmark regression/baseline context
When benchmark runner report is built
Then deterministic run plan entries and runner command metadata are emitted
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration output assembly
When artifact summary is finalized
Then `artifact_summary.benchmark_runner_report` is always present
```

## AC-3: Deterministic Run Modes
```gherkin
Given healthy, recovery, and regression contexts
When runner report is evaluated
Then run mode and planned scenario ids are deterministic
```

## AC-4: Regression Stability
```gherkin
Given benchmark runner report integration
When targeted and full test suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add benchmark runner report helper</name>
  <files>src/nixvibe/orchestrator/benchmark_runner.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic run-mode selection and scenario execution plan report with stable command strings.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.benchmark_runner import build_benchmark_runner_report"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline runner report output</name>
  <files>src/nixvibe/orchestrator/pipeline.py</files>
  <action>
    Emit `artifact_summary.benchmark_runner_report` after scenario catalog and regression/baseline contracts are available.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests/docs and verify full suite</name>
  <files>tests/orchestrator/test_benchmark_runner_report.py, tests/orchestrator/test_acceptance_flows.py, README.md</files>
  <action>
    Add deterministic report tests for baseline, recovery, and regression run modes; document phase 14 runner-report slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/14-benchmark-scenario-harness-and-reports/14-02-SUMMARY.md`
</output>
