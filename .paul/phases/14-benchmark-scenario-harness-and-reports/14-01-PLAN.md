---
phase: 14-benchmark-scenario-harness-and-reports
plan: 01
type: execute
wave: 1
depends_on: [13-03]
files_modified:
  - src/nixvibe/orchestrator/benchmark_scenarios.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_benchmark_scenario_catalog.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Add benchmark scenario fixture catalog and loader contract for phase 14 harness kickoff.

## Purpose
Provide deterministic scenario inventory/selection metadata so later benchmark runner can execute stable fixtures.

## Output
- `benchmark_scenario_catalog` contract helper (`benchmark-scenario-catalog/v1`)
- Scenario fixture loader API for deterministic lookup
- Pipeline integration at `artifact_summary.benchmark_scenario_catalog`
- Regression tests for route and regression-aware scenario selection
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/13-execution-telemetry-and-benchmark-contracts/13-03-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/benchmark.py
@src/nixvibe/orchestrator/telemetry_regression.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Scenario Catalog Contract
```gherkin
Given route/mode and benchmark context
When benchmark scenario catalog is built
Then deterministic scenario inventory and recommended selections are emitted
```

## AC-2: Loader Contract
```gherkin
Given scenario identifier lookup
When loader resolves a fixture
Then deterministic scenario metadata is returned or a clear error is raised
```

## AC-3: Pipeline Integration
```gherkin
Given orchestration output assembly
When artifact summary is finalized
Then `artifact_summary.benchmark_scenario_catalog` is always present
```

## AC-4: Regression Stability
```gherkin
Given scenario catalog integration
When targeted and full test suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add benchmark scenario catalog and loader helper</name>
  <files>src/nixvibe/orchestrator/benchmark_scenarios.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic fixture catalog, recommended selection logic, and fixture lookup API.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.benchmark_scenarios import build_benchmark_scenario_catalog, load_benchmark_scenario"</verify>
  <done>AC-1 and AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline scenario catalog output</name>
  <files>src/nixvibe/orchestrator/pipeline.py</files>
  <action>
    Emit `artifact_summary.benchmark_scenario_catalog` using run route/mode and benchmark regression context.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests/docs and verify full suite</name>
  <files>tests/orchestrator/test_benchmark_scenario_catalog.py, tests/orchestrator/test_acceptance_flows.py, README.md</files>
  <action>
    Add deterministic catalog/loader tests and route/regression selection coverage; document phase 14 scenario-catalog slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/14-benchmark-scenario-harness-and-reports/14-01-SUMMARY.md`
</output>
