---
phase: 14-benchmark-scenario-harness-and-reports
plan: 03
type: execute
wave: 1
depends_on: [14-02]
files_modified:
  - src/nixvibe/orchestrator/benchmark_snapshot.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_benchmark_baseline_snapshot.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Add deterministic baseline snapshot generation and regression checks for phase 14 closeout.

## Purpose
Convert runner/baseline/regression signals into a stable snapshot contract that supports milestone trend tracking and cross-contract consistency validation.

## Output
- `benchmark_baseline_snapshot` contract helper (`benchmark-baseline-snapshot/v1`)
- Deterministic regression consistency checks across baseline, telemetry, scenario, and runner contracts
- Pipeline integration at `artifact_summary.benchmark_baseline_snapshot`
- Regression tests for baseline-candidate, recovery-needed, and regression-investigate snapshot buckets
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/14-benchmark-scenario-harness-and-reports/14-02-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/benchmark.py
@src/nixvibe/orchestrator/telemetry_regression.py
@src/nixvibe/orchestrator/benchmark_scenarios.py
@src/nixvibe/orchestrator/benchmark_runner.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Baseline Snapshot Contract
```gherkin
Given telemetry, baseline, scenario, and runner contracts
When baseline snapshot is built
Then deterministic snapshot metadata and regression consistency checks are emitted
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration output assembly
When artifact summary is finalized
Then `artifact_summary.benchmark_baseline_snapshot` is always present
```

## AC-3: Deterministic Snapshot Buckets
```gherkin
Given baseline-ready, recovery-needed, and regression contexts
When snapshot contract is evaluated
Then trend bucket classification and recordability are deterministic
```

## AC-4: Regression Stability
```gherkin
Given baseline snapshot integration
When targeted and full test suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add baseline snapshot helper and exports</name>
  <files>src/nixvibe/orchestrator/benchmark_snapshot.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic baseline snapshot contract with cross-contract regression checks and stable snapshot id generation.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.benchmark_snapshot import build_benchmark_baseline_snapshot"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline baseline snapshot output</name>
  <files>src/nixvibe/orchestrator/pipeline.py</files>
  <action>
    Emit `artifact_summary.benchmark_baseline_snapshot` after baseline/regression/scenario/runner contracts are assembled.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests/docs and verify full suite</name>
  <files>tests/orchestrator/test_benchmark_baseline_snapshot.py, tests/orchestrator/test_acceptance_flows.py, README.md</files>
  <action>
    Add deterministic snapshot bucket + mismatch regression tests and document phase 14 baseline snapshot closeout slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/14-benchmark-scenario-harness-and-reports/14-03-SUMMARY.md`
</output>
