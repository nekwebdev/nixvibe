---
phase: 01-core-policy-and-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .agents/carl/nixvibe-domain.md
  - .codex/config.toml
  - .mcp.json
  - docs/contracts/local-mcp-contract.md
  - docs/contracts/specialist-output-schema.md
autonomous: true
---

<objective>
## Goal
Define and lock Phase 1 policy/contracts artifacts that control routing, safety gates, local MCP enforcement, and specialist output schema.

## Purpose
Create deterministic foundation for nixvibe behavior before orchestration implementation starts, preventing policy drift and environment-specific failures.

## Output
Committed policy/contracts documents and config contracts at fixed paths:
- `.agents/carl/nixvibe-domain.md`
- `.codex/config.toml`
- `.mcp.json`
- `docs/contracts/local-mcp-contract.md`
- `docs/contracts/specialist-output-schema.md`
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@../../projects/nixvibe/PLANNING.md

## Source Files
@README.md
</context>

<acceptance_criteria>

## AC-1: CARL Domain Locked
```gherkin
Given nixvibe Phase 1 policy work
When `.agents/carl/nixvibe-domain.md` is created
Then it defines route selection, write-mode gates, conflict policy order, validation gates, and user-skill adaptation rules
```

## AC-2: Local MCP Contracts Locked
```gherkin
Given local MCP is required for both Codex and Claude
When contract files/config are created and documented
Then Codex `.codex/config.toml` and Claude `.mcp.json` requirements are explicit, including `.codex` file-to-directory migration behavior
```

## AC-3: Specialist Output Schema Locked
```gherkin
Given specialist agents return structured artifacts
When `docs/contracts/specialist-output-schema.md` is created
Then required fields, merge semantics, and conflict resolution inputs are fully specified for implementation
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Author CARL policy domain file</name>
  <files>.agents/carl/nixvibe-domain.md</files>
  <action>
    Create CARL source-of-truth file at `.agents/carl/nixvibe-domain.md`.
    Include sections for:
    - route selection policy (`init` vs `audit`)
    - write modes (`advice`, `propose`, `apply`) and confirm-first gates
    - conflict priority (`safety > correctness > reversibility > simplicity > user preference > style`)
    - required validation gates (`nix flake check`, `nix fmt`)
    - behavior adaptation across user skill spectrum
    Keep language implementation-ready, no ambiguous rule text.
  </action>
  <verify>test -f .agents/carl/nixvibe-domain.md && rg -n "route|write mode|conflict|validation|skill" .agents/carl/nixvibe-domain.md</verify>
  <done>AC-1 satisfied: CARL domain file exists and contains all required policy sections.</done>
</task>

<task type="auto">
  <name>Task 2: Lock Codex/Claude local MCP contract</name>
  <files>.codex/config.toml, .mcp.json, docs/contracts/local-mcp-contract.md</files>
  <action>
    Create local MCP contract artifacts:
    - `.codex/config.toml` with required nix validation MCP configuration for project-local execution.
    - `.mcp.json` with required local server configuration for Claude.
    - `docs/contracts/local-mcp-contract.md` documenting:
      - forced-local rule (no global-only fallback)
      - environment root portability via `$CODEX_HOME` and `$CLAUDE_CONFIG_DIR`
      - `.codex` read-only file bug migration path (delete file, create `.codex/` directory)
      - expected health checks and failure behavior.
    Keep config samples minimal and executable.
  </action>
  <verify>test -f .codex/config.toml && test -f .mcp.json && test -f docs/contracts/local-mcp-contract.md && rg -n "forced|local|CODEX_HOME|CLAUDE_CONFIG_DIR|read-only|.codex" docs/contracts/local-mcp-contract.md</verify>
  <done>AC-2 satisfied: local MCP requirements are explicit and codified for both tools.</done>
</task>

<task type="auto">
  <name>Task 3: Define specialist output schema and merge contract</name>
  <files>docs/contracts/specialist-output-schema.md</files>
  <action>
    Create schema contract for specialist agent outputs.
    Define:
    - required payload fields (agent id, task scope, findings, recommendations, confidence, risks)
    - artifact sections expected by orchestrator
    - merge semantics and tie-break behavior that feed conflict policy
    - error/null payload handling
    - minimum validation checks before orchestrator emits final response.
    Include at least one concrete normalized example payload.
  </action>
  <verify>test -f docs/contracts/specialist-output-schema.md && rg -n "schema|agent|confidence|risk|merge|example" docs/contracts/specialist-output-schema.md</verify>
  <done>AC-3 satisfied: schema and merge behavior are implementation-ready and testable.</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Runtime orchestration implementation code (Phase 2 concern)
- Validation runner implementation code (Phase 3 concern)
- Existing phase ordering in `.paul/ROADMAP.md`

## SCOPE LIMITS
- No UI, CLI command surface, or end-user interaction implementation
- No actual scaffold/refactor generation logic in this plan
- No global environment mutation outside project-local contracts

</boundaries>

<verification>
Before declaring plan complete:
- [ ] All five files in `files_modified` exist
- [ ] Acceptance criteria AC-1 through AC-3 map to completed tasks
- [ ] Policy rules in CARL file exactly match locked planning decisions
- [ ] Local MCP contract docs include `.codex` migration behavior
- [ ] Specialist schema includes merge semantics and example payload
</verification>

<success_criteria>
- All tasks completed
- AC-1, AC-2, AC-3 validated
- Artifacts are clear enough for direct APPLY execution without re-asking policy questions
</success_criteria>

<output>
After completion, create `.paul/phases/01-core-policy-and-contracts/01-01-SUMMARY.md`
</output>

