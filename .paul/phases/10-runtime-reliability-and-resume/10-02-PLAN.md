---
phase: 10-runtime-reliability-and-resume
plan: 02
type: execute
wave: 1
depends_on: [10-01]
files_modified:
  - src/nixvibe/orchestrator/checkpoint.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_resume_checkpoint.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Add a resume-safe checkpoint contract to orchestration results.

## Purpose
Provide deterministic checkpoint metadata so interrupted or degraded runs can resume with explicit safety signals and next actions.

## Output
- `resume_checkpoint` contract builder
- Pipeline emission at `artifact_summary.resume_checkpoint`
- Deterministic resume-stage mapping
- Regression tests for success, validation failure, specialist invalid/error, and guardrail-escalated paths
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/10-runtime-reliability-and-resume/10-01-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/failure.py
@src/nixvibe/orchestrator/release.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Resume Checkpoint Contract
```gherkin
Given pipeline completion metadata
When resume checkpoint output is computed
Then a deterministic checkpoint contract is emitted with resume requirements and next-safe action
```

## AC-2: Resume Stage Mapping Coverage
```gherkin
Given validation, specialist runtime/payload, and escalation signals
When checkpoint stage is resolved
Then resume stage mapping is deterministic and stable
```

## AC-3: Clean Propose Run Behavior
```gherkin
Given a clean propose-mode run
When release readiness fails only on mode-apply gate
Then resume is not required by default
```

## AC-4: Regression Stability
```gherkin
Given resume checkpoint integration
When targeted and full test suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add checkpoint contract helper</name>
  <files>src/nixvibe/orchestrator/checkpoint.py</files>
  <action>
    Implement deterministic checkpoint contract fields, stage mapping, and resume-required policy.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.checkpoint import build_resume_checkpoint"</verify>
  <done>AC-1 and AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline output</name>
  <files>src/nixvibe/orchestrator/pipeline.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Emit `artifact_summary.resume_checkpoint` and export the helper.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests and docs</name>
  <files>tests/orchestrator/test_resume_checkpoint.py, tests/orchestrator/test_acceptance_flows.py, README.md</files>
  <action>
    Add regression coverage and document the new reliability contract slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/10-runtime-reliability-and-resume/10-02-SUMMARY.md`
</output>
