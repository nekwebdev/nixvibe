---
phase: 10-runtime-reliability-and-resume
plan: 03
type: execute
wave: 1
depends_on: [10-02]
files_modified:
  - src/nixvibe/orchestrator/retry.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_retry_backoff_guardrails.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Add deterministic retry/backoff orchestration guardrails.

## Purpose
Constrain automatic recovery to bounded windows and explicit stop conditions so retries stay safe and predictable.

## Output
- `retry_backoff_guardrails` contract builder
- Pipeline emission at `artifact_summary.retry_backoff_guardrails`
- Deterministic retry policy mapping by failure class/stage
- Regression tests for success, degraded, specialist-runtime, and blocked/human-confirmation paths
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/10-runtime-reliability-and-resume/10-02-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/failure.py
@src/nixvibe/orchestrator/checkpoint.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Retry/Backoff Guardrail Contract
```gherkin
Given run failure and resume checkpoint metadata
When retry guardrails are computed
Then deterministic retry bounds and backoff strategy are emitted
```

## AC-2: Policy Mapping Coverage
```gherkin
Given degraded, failed-runtime, blocked, and clean scenarios
When retry policy is emitted
Then automatic retry windows are bounded and human-review paths are explicit
```

## AC-3: Pipeline Integration
```gherkin
Given orchestration output
When artifact summary is finalized
Then `retry_backoff_guardrails` is included in output contract
```

## AC-4: Regression Stability
```gherkin
Given retry/backoff guardrail integration
When targeted and full tests run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add retry/backoff contract helper</name>
  <files>src/nixvibe/orchestrator/retry.py</files>
  <action>
    Implement deterministic guardrail policy for automatic retry windows, backoff schedules, and stop conditions.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.retry import build_retry_backoff_guardrails"</verify>
  <done>AC-1 and AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline output</name>
  <files>src/nixvibe/orchestrator/pipeline.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Emit `artifact_summary.retry_backoff_guardrails` and export helper.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests and docs</name>
  <files>tests/orchestrator/test_retry_backoff_guardrails.py, tests/orchestrator/test_acceptance_flows.py, README.md</files>
  <action>
    Add regression coverage and document retry/backoff guardrail behavior.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/10-runtime-reliability-and-resume/10-03-SUMMARY.md`
</output>
