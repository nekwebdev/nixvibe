---
phase: 10-runtime-reliability-and-resume
plan: 01
type: execute
wave: 1
depends_on: [09-03]
files_modified:
  - src/nixvibe/orchestrator/failure.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_run_failure_classification.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Add deterministic run failure classification output to orchestration results.

## Purpose
Provide reliability-oriented failure class/severity metadata so downstream resume and retry logic can make consistent decisions.

## Output
- `run_failure_classification` contract builder
- Pipeline emission at `artifact_summary.run_failure_classification`
- Severity mapping across blocked/failed/degraded/none outcomes
- Regression tests for validation, specialist invalid/error, and success paths
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/09-operator-surfaces-and-release-ops/09-03-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/pipeline.py
@src/nixvibe/orchestrator/manifest.py
@src/nixvibe/orchestrator/release.py
</context>

<acceptance_criteria>

## AC-1: Failure Classification Contract
```gherkin
Given pipeline completion metadata
When run failure classification is computed
Then a deterministic contract is emitted with class and severity
```

## AC-2: Severity Mapping Coverage
```gherkin
Given blocked/guarded/advisory and specialist error/invalid scenarios
When failure classification is emitted
Then class/severity mapping is deterministic and stable
```

## AC-3: Pipeline Integration
```gherkin
Given orchestration output
When artifact summary is finalized
Then `run_failure_classification` is included in output contract
```

## AC-4: Regression Stability
```gherkin
Given run failure classification integration
When targeted and full tests run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add failure classification helper</name>
  <files>src/nixvibe/orchestrator/failure.py</files>
  <action>
    Implement deterministic classification/severity mapping from run-manifest and escalation signals.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.failure import build_run_failure_classification"</verify>
  <done>AC-1 and AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline output</name>
  <files>src/nixvibe/orchestrator/pipeline.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Emit `artifact_summary.run_failure_classification` and export helper.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests and docs</name>
  <files>tests/orchestrator/test_run_failure_classification.py, tests/orchestrator/test_acceptance_flows.py, README.md</files>
  <action>
    Add deterministic regression matrix and document the new reliability contract.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/10-runtime-reliability-and-resume/10-01-SUMMARY.md`
</output>
