---
phase: 16-benchmark-trend-persistence-and-deltas
plan: 01
type: execute
wave: 1
depends_on: [15-03]
files_modified:
  - src/nixvibe/orchestrator/benchmark_trend.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_benchmark_trend_entry.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Add benchmark trend-entry contract for v0.6 phase 16 kickoff.

## Purpose
Convert per-run benchmark signals into deterministic trend entries that can be compared in upcoming delta-analysis slices.

## Output
- `benchmark_trend_entry` contract helper (`benchmark-trend-entry/v1`)
- Deterministic trend status bands and trend-key generation
- Pipeline integration at `artifact_summary.benchmark_trend_entry`
- Regression tests for improving, degraded, and blocked trend-entry paths
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/15-outcome-tracking-and-milestone-closeout/15-03-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/benchmark_snapshot.py
@src/nixvibe/orchestrator/outcome_scorecard.py
@src/nixvibe/orchestrator/benchmark_release.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Trend Entry Contract
```gherkin
Given baseline snapshot, outcome scorecard, and benchmark release contracts
When trend entry is built
Then deterministic trend status and trend key metadata are emitted
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration output assembly
When artifact summary is finalized
Then `artifact_summary.benchmark_trend_entry` is always present
```

## AC-3: Deterministic Trend Status Paths
```gherkin
Given healthy, degraded, and missing-contract contexts
When trend entry is evaluated
Then trend status and failed check IDs are deterministic
```

## AC-4: Regression Stability
```gherkin
Given benchmark trend integration
When targeted and full test suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add benchmark trend entry helper and exports</name>
  <files>src/nixvibe/orchestrator/benchmark_trend.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic trend-key, trend-status, and dependency-check output contract.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.benchmark_trend import build_benchmark_trend_entry"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline trend entry output</name>
  <files>src/nixvibe/orchestrator/pipeline.py</files>
  <action>
    Emit `artifact_summary.benchmark_trend_entry` after benchmark release readiness and outcome scorecard contracts.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests/docs and verify full suite</name>
  <files>tests/orchestrator/test_benchmark_trend_entry.py, tests/orchestrator/test_acceptance_flows.py, README.md</files>
  <action>
    Add deterministic trend-path tests and document phase 16 trend entry slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/16-benchmark-trend-persistence-and-deltas/16-01-SUMMARY.md`
</output>
