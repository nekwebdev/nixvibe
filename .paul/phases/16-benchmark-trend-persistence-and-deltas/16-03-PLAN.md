---
phase: 16-benchmark-trend-persistence-and-deltas
plan: 03
type: execute
wave: 1
depends_on: [16-02]
files_modified:
  - src/nixvibe/orchestrator/benchmark_trend_history.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_benchmark_trend_history.py
  - tests/orchestrator/test_phase16_trend_persistence_acceptance.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
  - .paul/ROADMAP.md
  - .paul/PROJECT.md
  - .paul/STATE.md
  - .paul/paul.json
autonomous: true
---

<objective>
## Goal
Close phase 16 with end-to-end benchmark trend persistence acceptance coverage.

## Purpose
Advance from isolated delta computation to deterministic cross-run trend persistence flow needed by upcoming alert and policy-gate phases.

## Output
- `benchmark_trend_history` contract (`benchmark-trend-history/v1`)
- Pipeline integration for history-aware trend delta evaluation
- End-to-end acceptance coverage for sequential run persistence
- Phase 16 closeout metadata transitioned to phase 17 readiness
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/16-benchmark-trend-persistence-and-deltas/16-02-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/benchmark_trend.py
@src/nixvibe/orchestrator/benchmark_trend_delta.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Trend History Persistence Contract
```gherkin
Given prior benchmark trend entries and a current trend entry
When trend history is built
Then deterministic previous-entry selection and bounded persisted history are emitted
```

## AC-2: Pipeline History-Aware Delta Integration
```gherkin
Given pipeline execution with optional prior trend history
When artifact summary is finalized
Then `benchmark_trend_history` and history-aware `benchmark_trend_delta` are emitted
```

## AC-3: End-to-End Trend Persistence Acceptance
```gherkin
Given sequential benchmark-capable runs
When prior trend history from run N is provided to run N+1
Then first run reports `no_baseline` and second run reports a non-baseline delta with previous context
```

## AC-4: Regression Stability and Phase Closeout Readiness
```gherkin
Given phase 16 completion updates
When targeted and full suites run
Then all tests pass and phase metadata is ready for phase 17 planning
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add benchmark trend history helper and exports</name>
  <files>src/nixvibe/orchestrator/benchmark_trend_history.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic benchmark trend history contract with previous-entry selection and bounded retention.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.benchmark_trend_history import build_benchmark_trend_history"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire history-aware delta flow in pipeline</name>
  <files>src/nixvibe/orchestrator/pipeline.py, tests/orchestrator/test_acceptance_flows.py</files>
  <action>
    Extend pipeline input/output to carry trend history and evaluate benchmark trend delta against persisted previous entry.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add trend persistence acceptance tests, docs, and closeout metadata</name>
  <files>tests/orchestrator/test_benchmark_trend_history.py, tests/orchestrator/test_phase16_trend_persistence_acceptance.py, README.md, .paul/ROADMAP.md, .paul/PROJECT.md, .paul/STATE.md, .paul/paul.json</files>
  <action>
    Add deterministic persistence/acceptance tests, document phase16 closeout slice, and reconcile phase status to phase17 readiness.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/16-benchmark-trend-persistence-and-deltas/16-03-SUMMARY.md`
</output>
