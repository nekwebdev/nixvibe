---
phase: 16-benchmark-trend-persistence-and-deltas
plan: 02
type: execute
wave: 1
depends_on: [16-01]
files_modified:
  - src/nixvibe/orchestrator/benchmark_trend_delta.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_benchmark_trend_delta.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Add benchmark trend-delta contract for prior-vs-current trend interpretation.

## Purpose
Advance phase 16 from isolated trend entries to deterministic delta semantics needed for alerting and policy-gate phases.

## Output
- `benchmark_trend_delta` contract helper (`benchmark-trend-delta/v1`)
- Deterministic delta statuses (`no_baseline`, `improvement`, `stable`, `regression`, `blocked`)
- Pipeline integration at `artifact_summary.benchmark_trend_delta`
- Regression tests for no-baseline, improvement, regression, and blocked delta paths
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/16-benchmark-trend-persistence-and-deltas/16-01-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/benchmark_trend.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Trend Delta Contract
```gherkin
Given current and optional previous trend entries
When trend delta is built
Then deterministic delta metadata and status classifications are emitted
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration output assembly
When artifact summary is finalized
Then `artifact_summary.benchmark_trend_delta` is always present
```

## AC-3: Deterministic Delta Status Paths
```gherkin
Given no-baseline, improvement, regression, and blocked contexts
When trend delta is evaluated
Then delta status and failed check IDs are deterministic
```

## AC-4: Regression Stability
```gherkin
Given trend delta integration
When targeted and full test suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add benchmark trend delta helper and exports</name>
  <files>src/nixvibe/orchestrator/benchmark_trend_delta.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic trend-delta contract with prior-entry optional semantics.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.benchmark_trend_delta import build_benchmark_trend_delta"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pipeline trend delta output</name>
  <files>src/nixvibe/orchestrator/pipeline.py</files>
  <action>
    Emit `artifact_summary.benchmark_trend_delta` using current trend entry contract output.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests/docs and verify full suite</name>
  <files>tests/orchestrator/test_benchmark_trend_delta.py, tests/orchestrator/test_acceptance_flows.py, README.md</files>
  <action>
    Add deterministic delta path tests and document phase 16 trend-delta slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/16-benchmark-trend-persistence-and-deltas/16-02-SUMMARY.md`
</output>
