---
phase: 06-guidance-ux-and-safety-guardrails
plan: 01
type: execute
wave: 1
depends_on: [05-03]
files_modified:
  - src/nixvibe/orchestrator/guidance.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_guidance_output.py
  - README.md
autonomous: true
---

<objective>
## Goal
Implement a skill-adaptive output contract for user-facing orchestration summaries.

## Purpose
Make guidance structure explicit and deterministic for novice, intermediate, and expert users without weakening existing safety/mode behavior.

## Output
- Guidance module with skill inference and profile mapping
- Pipeline integration exposing `artifact_summary.guidance`
- Contract fields for style/depth/section ordering and immediate next action
- Tests proving novice/intermediate/expert adaptation behavior
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/05-runtime-agent-execution-and-patch-orchestration/05-03-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/pipeline.py
@src/nixvibe/orchestrator/types.py
</context>

<acceptance_criteria>

## AC-1: Skill Profiling Contract
```gherkin
Given a user request
When guidance profiling runs
Then a deterministic skill level is inferred (novice/intermediate/expert)
```

## AC-2: Skill-Adaptive Summary Shape
```gherkin
Given inferred skill level and execution context
When orchestration result is produced
Then artifact summary includes a structured guidance contract with style/depth/sections
```

## AC-3: Immediate Next Action Continuity
```gherkin
Given selected mode and merge output
When guidance summary is emitted
Then guidance contract carries the same immediate next action used by the pipeline result
```

## AC-4: Regression Safety
```gherkin
Given existing orchestration behavior
When guidance contract is added
Then existing tests continue to pass and new tests cover adaptive behavior
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add guidance profiling module</name>
  <files>src/nixvibe/orchestrator/guidance.py</files>
  <action>
    Implement skill inference and guidance profile contract helpers.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.guidance import infer_skill_level"</verify>
  <done>AC-1 and AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate guidance contract into pipeline</name>
  <files>src/nixvibe/orchestrator/pipeline.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Emit `artifact_summary.guidance` and export guidance helpers for consumers.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator import build_guidance_summary"</verify>
  <done>AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests and docs</name>
  <files>tests/orchestrator/test_guidance_output.py, README.md</files>
  <action>
    Add contract tests for novice/intermediate/expert guidance adaptation and document the new slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/06-guidance-ux-and-safety-guardrails/06-01-SUMMARY.md`
</output>
