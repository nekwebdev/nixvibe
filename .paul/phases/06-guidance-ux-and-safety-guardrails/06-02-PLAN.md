---
phase: 06-guidance-ux-and-safety-guardrails
plan: 02
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - src/nixvibe/orchestrator/guidance.py
  - src/nixvibe/orchestrator/pipeline.py
  - tests/orchestrator/test_guidance_output.py
  - tests/orchestrator/test_guidance_remediation.py
  - README.md
autonomous: true
---

<objective>
## Goal
Harden safety/remediation guidance output for validation and conflict failure states.

## Purpose
Make remediation behavior explicit, structured, and deterministic so users always receive actionable recovery guidance when apply or merge safety gates fail.

## Output
- Structured remediation contract under `artifact_summary.guidance.remediation`
- Deterministic remediation categories for:
  - pre-write validation failure
  - post-write validation failure
  - critical contradiction conflict fallback
- Pipeline wiring for validation-failure stage and conflict-forced-propose context
- Regression tests for remediation contract behavior
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/06-guidance-ux-and-safety-guardrails/06-01-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/guidance.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Validation Remediation Contract
```gherkin
Given apply flow validation fails
When guidance summary is produced
Then remediation metadata includes category, blockers, and explicit recovery actions
```

## AC-2: Conflict Remediation Contract
```gherkin
Given critical contradictions force propose mode
When guidance summary is produced
Then remediation metadata signals conflict-critical recovery requirements
```

## AC-3: Deterministic Default Contract
```gherkin
Given no active remediation condition
When guidance summary is produced
Then remediation contract is explicitly `none` and non-required
```

## AC-4: Regression Safety
```gherkin
Given existing orchestration flows
When remediation contract integration lands
Then full suite remains green and remediation tests cover failure classes
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Extend guidance remediation model</name>
  <files>src/nixvibe/orchestrator/guidance.py</files>
  <action>
    Add remediation categories/actions/blockers for validation and critical conflict failures.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.guidance import build_guidance_summary"</verify>
  <done>AC-1, AC-2, AC-3 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire remediation context from pipeline</name>
  <files>src/nixvibe/orchestrator/pipeline.py</files>
  <action>
    Compute validation failure stage and conflict-forced-propose signals and pass them to guidance contract builder.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>Remediation integration path complete.</done>
</task>

<task type="auto">
  <name>Task 3: Add remediation tests and docs</name>
  <files>tests/orchestrator/test_guidance_output.py, tests/orchestrator/test_guidance_remediation.py, README.md</files>
  <action>
    Add targeted remediation tests and document the new safety/remediation slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/06-guidance-ux-and-safety-guardrails/06-02-SUMMARY.md`
</output>
