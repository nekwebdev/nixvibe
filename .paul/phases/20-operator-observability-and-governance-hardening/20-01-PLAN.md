---
phase: 20-operator-observability-and-governance-hardening
plan: 01
type: execute
wave: 1
depends_on: [19-03]
files_modified:
  - src/nixvibe/orchestrator/operator_observability_digest.py
  - src/nixvibe/orchestrator/pipeline.py
  - src/nixvibe/orchestrator/__init__.py
  - tests/orchestrator/test_operator_observability_digest.py
  - tests/orchestrator/test_acceptance_flows.py
  - README.md
autonomous: true
---

<objective>
## Goal
Introduce operator observability digest contract for phase 20.

## Purpose
Aggregate run manifest, audit trail, telemetry, and release policy execution signals into a deterministic operator-facing observability digest.

## Output
- `operator_observability_digest` contract (`operator-observability-digest/v1`)
- Pipeline integration at `artifact_summary.operator_observability_digest`
- Deterministic observability banding (`healthy`, `attention`, `critical`, `degraded`)
- Regression tests for observability digest paths
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@README.md

## Prior Work
@.paul/phases/19-release-automation-and-policy-execution/19-03-SUMMARY.md

## Runtime Baseline
@src/nixvibe/orchestrator/manifest.py
@src/nixvibe/orchestrator/audittrail.py
@src/nixvibe/orchestrator/release_policy_execution.py
@src/nixvibe/orchestrator/pipeline.py
</context>

<acceptance_criteria>

## AC-1: Operator Observability Digest Contract
```gherkin
Given run manifest, audit-trail, telemetry, and release policy execution outcomes
When observability digest evaluates
Then deterministic observability band and operator focus metadata is emitted
```

## AC-2: Pipeline Integration
```gherkin
Given orchestration artifact assembly
When artifact summary finalizes
Then `artifact_summary.operator_observability_digest` is always present
```

## AC-3: Digest Path Determinism
```gherkin
Given healthy, attention, critical, and degraded contexts
When observability digest evaluates
Then banding and action recommendations remain deterministic
```

## AC-4: Regression Stability
```gherkin
Given observability digest integration
When targeted and full suites run
Then all tests pass
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add observability digest helper and exports</name>
  <files>src/nixvibe/orchestrator/operator_observability_digest.py, src/nixvibe/orchestrator/__init__.py</files>
  <action>
    Implement deterministic operator observability digest contract that aggregates operational signals into banded operator guidance.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.operator_observability_digest import build_operator_observability_digest"</verify>
  <done>AC-1 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Wire observability digest in pipeline</name>
  <files>src/nixvibe/orchestrator/pipeline.py, tests/orchestrator/test_acceptance_flows.py</files>
  <action>
    Emit `artifact_summary.operator_observability_digest` using run manifest, audit trail, telemetry, and release policy execution metadata.
  </action>
  <verify>PYTHONPATH=src python -c "from nixvibe.orchestrator.pipeline import run_pipeline"</verify>
  <done>AC-2 satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Add observability digest tests and docs</name>
  <files>tests/orchestrator/test_operator_observability_digest.py, README.md</files>
  <action>
    Add deterministic digest-path tests and document phase20 observability digest slice.
  </action>
  <verify>python -m unittest discover -s tests -p 'test_*.py' -v</verify>
  <done>AC-3 and AC-4 satisfied.</done>
</task>

</tasks>

<output>
After completion, create `.paul/phases/20-operator-observability-and-governance-hardening/20-01-SUMMARY.md`
</output>
